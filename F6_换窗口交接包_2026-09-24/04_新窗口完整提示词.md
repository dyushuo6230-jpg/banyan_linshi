# 可复制到新窗口的完整提示词

请继续 Banyan / 榕树 AI 的 F6 架构盘点。先读我上传的 `F6_换窗口交接包_2026-09-24.zip` 中的入口文档 `00_先读我.md` 和 `F6_D01-D08_原冻结条文/00_来源索引与批准链.md`，其余按需读取，再按需核对 Git 仓库当前文件。来源会话：https://chatgpt.com/share/6ab4e870-49f0-83ea-a1ab-7578ef197093 和 https://chatgpt.com/share/6ab4cc24-dc64-83ea-a62d-f4357f50f40e 。请以交接包为索引，不要一次展开整个仓库或全部长文档，以免会话过重。

**身份与状态**：仓库是临时 AI 改造盘点资料仓。`榕树ai改造落地相关文档/Documentation_Packaging/` 中的 F1～F5 六件套是已存在的正式冻结来源；`banyan-framework/`、`.banyan/`、`project/`、`.banyan-refactor/` 分别是当前框架现实、项目实例现实、Legacy v3.1 项目资料、历史施工档案。F6-D01～D08 在上一会话上传的原交接包中标为 `HUMAN_APPROVED / Architecture Freeze PASS`，不要重新设计；F6-D09 仍为 `HUMAN_REVIEW`，没有得到我的最终批准；F6-D10 尚未形成完整最终候选。Implementation、RP2、Final Activation、Cursor Coding 均未授权。

**来源与核对**：交接包的 `F6_D01-D08_原冻结条文/` 已保存 D01～D08 逐项原条文、后续批准链、D02 总体冻结收口审计，以及当时上传的 `F6_CONFIRMED_FREEZE_PACK.md`、`F6_WORKING_HANDOVER_CHECKPOINT.md`、`F6_NEXT_WINDOW_START_HERE.md` 和完整交接提示词。原条文里的 `HUMAN_REVIEW` 是起草时原貌，后续批准记录才是最终状态。旧 VV-01～VV-30 的完整候选原文尚未单独定位；不要声称已完成旧 VV 逐条核对。旧会话的 `filecite` 需回到原会话或仓库原文件核查。

**当前主线**：继续 F6-D09，即 UI 一致性校准、设计偏离和交互/功能回归的验证治理。D09 负责解析目标、限定检查范围、采集实际证据、区分差异与偏离、检查受保护功能回归、输出可追溯验证结果；D10 后续才负责修改授权、修复执行、迭代控制和设计与代码同步。D09 旧候选稿在包内 `03_D09_旧候选稿_未批准.md`，它产生于最近的风险讨论之前，只能用来对账，不能直接冻结。

**最近讨论还需纳入**：顶层使用 UI Conformance Session（UI 一致性校准会话）；Visual Repair Loop（视觉修复循环）仅为兼容配置。默认按需启动、非阻塞。目标解析先判断当前有效 UI_SPEC，再判断受治理 Design Package，最后是用户明确指定的设计来源；只靠 PRD 不能直接发起视觉修复。检查与修复分离；现有页面只是现实证据，除非被明确指定为受保护基线。差异不等于偏离；像素差异只是证据。目标过期、权威冲突、证据不足时不能猜结论。修复后要检查受保护的 API、权限、业务状态、上传、删除等功能。

我还提出：一轮校准发现严重风险时，先记录为待决策项，继续处理其它互不依赖、已授权的低风险范围；本轮报告列出风险项。用户确认后，只处理被确认的项并做局部复验，处理结束后停止，不能自动发起下一轮。上一会话进一步建议 R0～R3 风险分级与既有决策治理路由，但**我尚未明确批准这套分级**；请把它当候选，与我讨论。风险等级不能赋予修改权限。当前不实现 AI 自主学习。

**你的第一步**：先简洁复述你读取到的状态、已确认与待确认的界线、D01～D08 原文所在位置及旧 VV 缺口。接着请用大白话、按组讨论 D09 尚需我拍板的事项；每次最多五个关键问题，英文术语首次出现时附中文，并用素材管理/上传弹框之类的实际场景举例。先讨论，不要直接把旧候选稿改成冻结文件。等我逐项明确说“确定”后，再生成新版完整 D09 候选给我审批；在我明确批准前，状态始终是 `HUMAN_REVIEW`，不要进入 D10 或写代码。
