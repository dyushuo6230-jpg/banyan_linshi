# 18.5-D Go/Gin Control Plane

推荐逻辑层次：

```text
cmd/banyan-control
internal/controlplane/http
internal/controlplane/dto
internal/controlplane/app
internal/runtimebridge
internal/webassets
```

Gin 只做 Transport。

HTTP DTO 必须和 Runtime Contract 做显式映射，不把 HTTP JSON 结构当 Runtime Canonical Model。
