# 18.5-F Embedded Frontend Distribution

Release pipeline：

```text
frontend install
frontend production build
generate asset SHA-256 manifest
copy/locate build output
go embed
go build
launch binary
request SPA
request static asset
request API
```

验证：

```text
release UI served without Node process
asset hashes match
SPA deep links pass
API routes remain API
```
