# 18.5-G Local-only / Security

默认：

```text
127.0.0.1
```

必须有：

```text
read/header/body limits
timeouts
typed errors
panic recovery
no shell interpolation
no public bind by default
```

当前项目仍：

```text
read-only / preflight / plan / dry-run
```

除非既有 Pilot Contract 明确授权其他行为；本阶段不得扩大。
