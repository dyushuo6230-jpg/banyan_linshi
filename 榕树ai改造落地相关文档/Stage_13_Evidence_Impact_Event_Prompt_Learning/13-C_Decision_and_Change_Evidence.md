# 13-C Decision / Change Evidence

必须支持：

```text
decision -> evidence
decision -> options
decision -> impact
change -> affected artifacts
change -> validation
change -> result
```

Human Decision 仍是独立证据类型，不能由 AI Summary 替代。
