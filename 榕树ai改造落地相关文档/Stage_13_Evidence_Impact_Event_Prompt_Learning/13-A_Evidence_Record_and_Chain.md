# 13-A Evidence Record / Chain

Evidence 不等于结论。

必须记录：

```text
what was observed
where it came from
when/as-of
authority
provenance
freshness
confidence
```

Evidence Chain 禁止缺跳跃地从 `INFERENCE` 直接到 `FACT`。
