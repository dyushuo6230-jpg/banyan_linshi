# 15-C CLI

CLI 最低语义：

```text
policy validate
policy compile
runtime preflight
git inspect
commit plan
commit execute --dry-run
commit execute --fixture
trace validate
provider list
```

不得提供绕过 Policy 的 `--force`。
