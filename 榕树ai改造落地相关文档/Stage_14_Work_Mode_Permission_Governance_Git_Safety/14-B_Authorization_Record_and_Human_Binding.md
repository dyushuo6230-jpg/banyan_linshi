# 14-B Authorization / Human Binding

Authorization 必须明确：

```text
who/what authorizes
which action
which target
which scope
which constraints
which decision
```

历史提交身份、Contributor Profile、Editor 类型均不能隐式授权。
