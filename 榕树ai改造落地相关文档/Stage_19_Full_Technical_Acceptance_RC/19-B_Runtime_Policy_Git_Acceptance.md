# 19-B Runtime / Policy / Git

Revalidate critical Stage15/14 invariants:

```text
unknown policy fields fail
hard blocks survive compilation
failed preconditions cannot ALLOW
identity precheck required
authorization required
no blind git add .
no secret-risk execution
commit capture requires trace
current project mutation remains zero
```
