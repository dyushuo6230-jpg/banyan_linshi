# 20-B Install / Build / Run Guide

Must separate:

```text
WebUI Viewer / Go host prerequisites
Full Banyan Runtime prerequisites
Developer build prerequisites
```

Explain:

```text
Node = frontend build-time dependency
Go = web-host build-time dependency
Python = Banyan Runtime dependency
```

Do not falsely state that the whole Banyan product is a single standalone Go binary.
