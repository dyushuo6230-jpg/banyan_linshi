# 20-F Governance / Security Guide

Document:

```text
Permission results
Authorization distinction
Git identity semantics
Secret metadata-only
Git mutation gates
Evidence != Authorization
Trace != Authorization
UI intent != Authorization
```
