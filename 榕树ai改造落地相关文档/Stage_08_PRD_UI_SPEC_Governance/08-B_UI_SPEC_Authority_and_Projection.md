# 08-B UI_SPEC Authority / Projection

UI_SPEC 承担：

```text
UI structure
interaction contract
state representation
responsive constraints
design evidence interpretation
implementation-facing UI constraints
```

UI_SPEC 中若必须提及业务规则：

```text
reference + minimal context
```

不能形成第二份完整业务真源。
