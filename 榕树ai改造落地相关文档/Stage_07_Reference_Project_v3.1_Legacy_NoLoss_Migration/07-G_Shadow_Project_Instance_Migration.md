# 07-G Shadow Project Instance Migration

只在 Stage 07 Run 内建立 Shadow Model：

```text
project instance
source mappings
overlays
profiles
provider bindings
artifact registry
workflow relations
```

用途：

```text
验证设计是否足够承载当前项目
```

不是正式 `.banyan/`。
