# 07-J Migration Rollback / Recovery

未来真实迁移前必须：

```text
checkpoint
source hashes
mapping snapshot
reference graph snapshot
target isolated validation
rollback plan
single writable truth switch
```

当前 Stage 07 只验证计划是否完整。
