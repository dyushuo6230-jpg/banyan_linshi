# 16-D Project Safety / Git UI

允许显示：

```text
branch
HEAD
staged count
unstaged count
untracked count
conflicted count
identity present
identity ambiguous
execution mode
```

不显示/不持久化：

```text
user.name
user.email
```

Stage16 当前项目 Mutation Controls：

```text
DISABLED
```
