# EFFECTIVE_UI_CONTRACT：源品 Partial 换皮（底栏 / 首页 / 分类 / 附近列表）

| 字段 | 值 |
|---|---|
| 文档名称 | 源品点名页换皮有效约束 |
| 文档编号 | UIC-gongyi_online-partial-001 |
| 版本 | 0.1.5 |
| 状态 | Active |
| 创建日期 | 2026-09-18 |
| 更新时间 | 2026-09-19 |
| 适用范围 | 仅下列四处；不约束 DEV-013/014 其它页面 |
| 规范 | UI-SPEC-gongyi_online 0.1.5 Approved（分类听本合同 + `category.vue`；附近听本合同 + `index-f.vue` 第 4 节覆盖） |
| 代码真源 | `go-uni-app-online` |
| 全局 UI_CONSTRAINT_MODE | 仍为 NONE（本文件只约束本次点名换皮） |

禁止业务路由 `/v2`。CSS 类名里已有的 `*-v2` 不是新业务轨。

## 点名页

| 页 | 路由 / 零件 | 对照图 | 等级 |
|---|---|---|---|
| 底栏 | `addon/shop/components/tabbar/index.vue` | 整栏图（个人页等页脚）+《Tab Bar交互》上半 | F3 |
| 首页 | `/addon/shop/pages/indexPage/index` | `公益优选-商城首页定稿.png` + 横向分类选中听同盒三件套 | F3 |
| 分类 | `/addon/shop/pages/indexPage/category` | `分类.png` + DEC-071 v1.3.0 / UI-DEC-005 / UI-DEC-006（听现码，不对原图米金条与外侧箭头） | F3 |
| 附近列表 | `/addon/shop/pages/nearby/index-f` | `MAT-GO-IMG-NEARBY` + UI_SPEC 0.1.5 第 4 节整页 PRD 覆盖 | F3（点名换皮 / 32A 之后） |

未点名页不得借本次改视觉。购物车页不像素；只保证底栏第四坑能进去。店详情不在本合同换皮范围。

## Locked 令牌（来源：tokens + UI-DEC-003）

| Token | 值 | 用途 |
|---|---|---|
| brand-gold | `#FFC31B` | 金顶；分类二级选中金底 |
| surface | `#FFFFFF` | 卡片 / 搜索条 / 底栏 |
| surface-page | `#F6F6F6` | 页底 |
| text-primary | `#222222` | 主文；金底上的字 |
| text-muted | `#989898` | 次文、底栏未选 |
| price | `#FD5800` | 现价 |
| accent-lime | `#A6F342` | 底栏侧边选中填色 |
| search-cta | `#111111` | 黑「搜索」钮 |

落地 750rpx = 逻辑 px × 2。系统 `chip` 选中浅金仍 locked；首页横向分类选中是覆盖，不是令牌升级。选中黑底 `#222222` 只给这一粒，不写入 tokens JSON。分类二级选中金底用 `brand-gold`，不抽新 locked 色。附近复用金顶与搜索黑钮，不抽新 locked 色。

## 覆盖（相对 PNG）

| 项 | 听谁 |
|---|---|
| 第四坑 | UI-DEC-002：文案「购物车」，进 `/addon/shop/pages/goods/cart`，图标用发现图 |
| 金主按钮 / 金胶囊字 | UI-DEC-003：深字压金 |
| 分类名称是否出现 | **DEC-071 v1.1.0：** 不编造假入口；平台类目树上有的一级照出，第一项不收窄、不加高成另一套格子 |
| 首页顶区轮播 | **听本次红框覆盖（32A）**：轮播铺满从频道行到公告顶下一点；搜索与购物袋悬浮在轮播上。不定稿金顶+独立 KV，循环不得把顶区还原成定稿 |
| 首页横向分类选中 | **UI-DEC-004 观感 + UI_SPEC 0.1.2 三件套。** 观感：黑底、青柠星、下三角、白字。实现：与未选中同盒（高 `56rpx`、圆角 `76rpx`、左右 `33rpx`、边 `2rpx`）；选中只改底/边为 `#222222`；星用 `MAT-GO-ASSET-HOME-CHIP-STAR`；三角 CSS，不算进盒子。组 A 整图只作抠星来源与观感参考。**禁止**把 `首页横向分类选中背景15` 当 `background-image` 并 `100% 100%` 拉伸。循环不得改回浅金，也不得改回整图拉伸。未选中仍白底细边 `#e8e8e8` |
| 分类页图层 | **DEC-071 v1.3.0 + UI-DEC-005。** 外底板听现码：16rpx 圆角、16rpx 内边距、奶油落到页灰。禁止米金条、禁止 `side-bg.png` 拉伸竖条。三级白卡圆角 15rpx。左栏与右侧间距 20rpx |
| 分类左栏 | **UI-DEC-006。** 未选中同高。选中：白边包金芯、白边在阴影内、箭头在金卡内、选中加高、10rpx 圆角。顶底淡入跟现码（顶 `#fff6d6`、底白）。循环不得改回外侧箭头或选中与未选同高 |
| 分类二级槽 | **现码。** 槽 64rpx / 胶囊 46rpx / 圆角 35rpx；选中金渐变白字 + CSS 三角 `bottom: -15rpx`。不铺 `#fff6d6`。组 B 禁止拉伸。循环不得改槽高导致三角离卡 |
| 分类三级圆图 | CSS grid 三列均分；`column-gap 16rpx` / `row-gap 40rpx`；圆 140rpx；白卡 15rpx。禁止 flex 换行成两列 |
| 分类全部分类 | 内容区 overlay；标题固定；列表 520rpx 可滚；选中金底深字 `#222`。循环不得改回不可滚或白字选中 |
| 中间 AI | 橙红圆形标，不得改青柠卡通 |
| 附近删除板块 | 文件名 + M07：超市便利、生鲜果蔬、优选超市、美团团购、数码百货、看病买药、鲜花绿植等图上有也不画、不对接 |
| 附近 CPS | M07：两行本页 `swiper` 轮播，每页最多 5+5；听平台目录。循环不得收成静宫格。跳转不进 32A |
| 附近顶灯片 | DEC-069：板块子列表，不是购物首页 banner |
| 附近筛选 | DEC-046：当前全部本地业态根 |
| 附近列表跳转 | M07：点店→`details-f`；点货→电商详情；不拉起店小程序。循环不改路由 |
| 附近店卡 | DEC-073 / REQ-134：头像/名/业态/营业/地址/招新开业。不画距离、驾车、让利、共益分发。定位行米数不当像素目标 |
| 附近底栏 | 附近循环只改 `index-f.vue`，不改 tabbar；标签下内边距保持 `30rpx` |
| 附近端口 | 只打 8035 |

## 必须

- 底栏五坑：购物 / 附近 / AI助手 / 购物车 / 我的；中间抬高；接口仍 8035
- 首页右上购物袋保留，进购物车
- 首页搜索黑钮仍听定稿（有黑「搜索」）
- 首页顶区结构听 32A 红框覆盖，见上表
- 首页横向分类选中听同盒三件套，见上表
- 分类右 3 列 **CSS grid** 圆图落在三级内底板白卡上；未选中同高；选中按 UI-DEC-006 加高
- 分类页图层 / 左栏选中现状 / 二级代码气泡 / 全部分类可滚 / 左栏顶底两态，见上表
- 附近 CPS 两行轮播；删除板块不画；店卡不画距离/让利；筛选项为本地业态根；顶灯片子列表

## 禁止

- 恢复发现业务
- 青柠涂按钮 / 价签 / 顶栏
- 把《Tab Bar交互》当完整底栏
- 改列表、搜索、商品详情、我的、店详情页视觉
- 把组 B 整图当分类二级选中底拉伸；把组 A 整图当首页选中底拉伸
- 编造树上没有的一级
- 借循环把首页选中改回浅金胶囊，或改回组 A 整图拉伸，或把顶区还原成定稿金顶+独立 KV
- 借循环把分类改回：无箭头、箭头在外、选中与未选同高、米金条、flex 两列、不可滚全部分类、`side-bg.png` 拉伸竖条、无二级三角、无外底板、二级米金遮罩、`.main` 无左距、全部分类选中白字
- 分类页 32A 用已冻结的 0.1.3 或《分类.png》当目标去改 `category.vue`。读序：本合同 → `UI_SPEC.md` 0.1.5 → 现码
- 借附近循环：补回删除板块；把 CPS 收成静宫格；补距离/驾车/让利；改底栏或店详情；改接口跳转；打 8032

## 分类页现码冻结

`go-uni-app-online/src/addon/shop/pages/indexPage/category.vue` 是分类视觉真源。32A 不得为贴图改上述覆盖项。单次 rpx 不升令牌。

## 附近列表

`go-uni-app-online/src/addon/shop/pages/nearby/index-f.vue`。金顶/搜索/灯片/店卡骨架可对图。第 4 节附近覆盖与 PNG 冲突时标豁免，不改代码去贴图。附近 32A 不得改 `tabbar/index.vue`。本合同同步 **不**自动开始附近换皮或 32A。
