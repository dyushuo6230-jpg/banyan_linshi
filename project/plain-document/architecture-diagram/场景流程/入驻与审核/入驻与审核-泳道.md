# 入驻与审核-泳道

这张图看申请人怎么从入驻页交到待审、平台怎么通过或驳回、通过之后权限落到哪套租户菜单。全业态同一套入驻页，没有第二套入驻产品。驳回不能改原单再送。

## 给谁看

开会投主链；测试按菱形走驳回、过期、同手机再提。细则在功能需求《租户入驻与审核》；页面在分端《租户入驻》《平台后台》入驻审核节。套哪套菜单见《租户后台与菜单》。

## 关键规则

- 选项只出现「入驻可选」开着的根；本地生活不选一级；在线商城必须选 1～3 个启用中的一级。
- 执照沿用现页，识别成功才能交。不问是否自营。
- 待审或已通过的同一手机不能再提；驳回只能提新单。没有菜单模板的业态不能通过。
- 存量待审当年没选业态：通过前补选，系统不猜。

## 图

```mermaid
flowchart TB
  subgraph 申请人["申请人"]
    direction TB
    aStart((申请人拿到有效入驻资格<br/>打开同一套入驻页，不是租户网页，也不是在线商城的顾客端「我的」))
    aPick["申请人从开着的根里选一个业态<br/>关掉的根看不见"]
    aMall["申请人必须勾 1～3 个启用中的一级<br/>个数不合或一个都没有：交不出去"]
    aFill["申请人上传执照、填店名、填 11 位联系电话<br/>身份证、行业许可证本期不升格必填"]
    aWait["申请人只看见申请变成待审<br/>这里不能发品、不能接单、进不了租户后台"]
    aRejectNew["原单已经结束，不能改这份再送<br/>申请人只能再拿有效资格提一份新的重新排队"]
    aDone((通过之后换租户后台办事<br/>入驻页不再经营))
  end

  subgraph 入驻页["入驻页"]
    direction TB
    pList["入驻页只列出入驻可选开着的根<br/>以后新增的本地根只要开着也走这一页"]
    pIsMall{"申请人选的是在线商城吗？"}
    pLocal["入驻页不出现可售一级<br/>本地生活不选经营类目下级"]
    pNoSelf["无此路径：表单不问是否自营<br/>平台不自己开自营店"]
    pOcr{"执照照片识别成功了吗？"}
    pOcrFail["提交走不通<br/>须换一张清晰原件再认"]
    pTicket{"入驻资格还有效吗？"}
    pTicketFail["提交走不通<br/>须重新取得入驻资格"]
    pPhone{"这个手机已经待审或已经通过了吗？"}
    pDup["不能再交一份<br/>权限仍停在原来那张单"]
    pSubmit["入驻页收下申请，状态变成待审<br/>票面上不要带着手机号给前端乱传"]
  end

  subgraph 平台审核["平台审核"]
    direction TB
    gOpen["平台运营从商户管理进入驻审核<br/>打开待审单办理，已通过和已驳回只看"]
    gLegacy{"这张存量待审当年没选业态吗？"}
    gFillBiz["通过前必须在本页补选业态，系统不猜<br/>在线商城还须补 1～3 个启用中的一级；补不了就驳回"]
    gTpl{"这个业态已经有菜单模板吗？"}
    gNoTpl["不能点通过<br/>申请人看不见模板配置"]
    gDecide{"通过还是驳回？"}
    gPass["申请变成已通过<br/>按该业态套该店菜单，经营权限落到租户后台"]
    gReject["申请变成已驳回<br/>这份单不能再当已入驻使用"]
  end

  subgraph 租户后台["租户后台"]
    direction TB
    mLogin["租户用入驻主账号登录同一套后台<br/>侧栏是该业态模板，不是四套网址"]
    mWork["这家店可以按业态发品、接单<br/>顾客端看不见审核过程"]
    mNo["无此路径：拿驳回单进后台经营"]
  end

  aStart --> pList --> aPick --> pIsMall
  pIsMall -->|是在线商城| aMall --> pNoSelf
  pIsMall -->|餐饮、美发、商超或以后新增的本地根| pLocal --> pNoSelf
  pNoSelf --> aFill --> pOcr
  pOcr -->|识别失败| pOcrFail
  pOcr -->|识别成功，展示企业名、法人、住所| pTicket
  pTicket -->|过期或无效| pTicketFail
  pTicket -->|仍有效| pPhone
  pPhone -->|已有待审或已通过| pDup
  pPhone -->|没有占用中的单| pSubmit --> aWait --> gOpen --> gLegacy
  gLegacy -->|没业态| gFillBiz --> gTpl
  gLegacy -->|已经有业态| gTpl
  gTpl -->|没有模板| gNoTpl
  gTpl -->|有模板| gDecide
  gDecide -->|驳回| gReject --> aRejectNew
  gReject --> mNo
  gDecide -->|通过| gPass --> mLogin --> mWork --> aDone
```

## 不画什么

- 不按餐饮 / 美发 / 商超画三套入驻产品；不把「到店」画成一种可选项。
- 无此路径：自营勾选、身份证/行业许可证必填、改原单再送、平台自己开自营店。
- 不画推广获客细链、店员出餐、抽佣死数字、端口和域名。
- 入驻套菜单的调用先后见同夹时序（本批不画）。
