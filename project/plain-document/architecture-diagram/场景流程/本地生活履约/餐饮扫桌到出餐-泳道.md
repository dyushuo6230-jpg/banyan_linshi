# 餐饮扫桌到出餐-泳道

这张图看拷贝源为餐饮的店：顾客扫合法桌、点单、付钱进该店、店员出餐后订单已完成。不进待评价。没有外卖。美发 / 商超不走这张图。

## 给谁看

开会投到店堂食主链；测试无效桌、未付关单、有售后不能出餐。细则在《本地生活怎么履约》；页面在分端《众享营销-本地生活》。钱进该店见《订单支付与结算》。

## 关键规则

- 履约跟拷贝源：种子餐饮及从餐饮拷来的新根走扫桌。到店是履约，不是一种业态。
- 认店：下单、支付不能信顾客端随便传的店号。无桌、桌号无效：不能按这张桌下单。
- 出餐或确认完成 → 已完成，不进待评价。已完成可写评价，评完仍是已完成。
- 有待处理售后时不能出餐。未付到期打开即关成已取消，到点不能再付。

## 图

```mermaid
flowchart TB
  subgraph 顾客["顾客"]
    direction TB
    cStart((顾客打开这一家餐饮店小程序<br/>不是众享源品在线商城的顾客端))
    cScan["顾客到店扫桌号，要按这张桌点单"]
    cMenu["顾客按这张桌点菜下单<br/>须已登录；认店只认这一家店"]
    cPay["顾客付钱"]
    cWait["顾客看见订单已付、等出餐<br/>现用口语可留已支付"]
    cDone["顾客看见订单已完成<br/>可以写评价，评完仍是已完成，没有待评价主状态"]
    cCancel((订单已取消<br/>不能再按这张未付单去付钱))
  end

  subgraph 店内小程序["店内小程序"]
    direction TB
    aDesk{"桌号合法、店里有这张桌吗？"}
    aNoDesk["不能按这张桌下单"]
    aFake{"顾客端有没有乱传别家店号？"}
    aBind["当前店以小程序绑的那一家为准<br/>不能下到别人店、不能付到别人店"]
    aDue{"待付款已经到期了吗？"}
    aClose["打开列表、详情或去支付时立刻关成已取消<br/>到点不能再付；后台定时仍会关"]
    aWaimai["无此路径：外卖配送下单"]
  end

  subgraph 支付通道["支付通道"]
    direction TB
    payOk["钱进该店账户<br/>不是付给平台，也不是在线商城的顾客端收款"]
  end

  subgraph 店员["店员"]
    direction TB
    dOpen["店员在同一套小程序店员页打开待出餐<br/>能看见顾客称呼；不是租户网页，也不是另一套独立 App"]
    dAfter{"这一单有待处理售后吗？"}
    dBlock["不能出餐<br/>须先办完售后"]
    dShip["店员出餐或确认完成<br/>履约权限落到本店，订单变成已完成"]
    dBoss["老板也可以在租户后台确认完成<br/>顾客看见的仍是这一家店的单变成已完成"]
  end

  cStart --> cScan --> aDesk
  aDesk -->|无桌或桌号无效| aNoDesk
  aDesk -->|合法桌| cMenu --> aFake --> aBind --> aDue
  aDue -->|已到期| aClose --> cCancel
  aDue -->|未到期| cPay --> payOk --> cWait --> dOpen --> dAfter
  dAfter -->|有待处理售后| dBlock
  dAfter -->|没有卡住的售后| dShip --> dBoss --> cDone
  cScan --> aWaimai
```

## 不画什么

- 不画美发 / 商超核销、在线商城的顾客端逛买、商城发货。
- 无此路径：外卖、第四种履约、待评价主状态、未付关成已完成。
- 重复扫同一桌的清台规则沿用现页，不编造。
- 预约只点到：默认仅餐饮且受首页模块归属，本图不把预约画成主链。
- 不画端口和域名。付到店细链、两种回调见以后支付夹。
