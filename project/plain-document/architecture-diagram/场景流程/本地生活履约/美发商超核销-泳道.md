# 美发商超核销-泳道

这张图看拷贝源为美发或商超的店：顾客先付、出一张主码、到店整单一次核销，订单已完成。店员小程序和租户后台都能核，不要画成只有网页。餐饮不走这张图。

## 给谁看

开会投到店核销主链；测试他店码、全额退后不能再核、有售后不能核。细则在《本地生活怎么履约》；页面在分端《众享营销-本地生活》《租户后台（本地生活）》订单节。

## 关键规则

- 种子美发 / 商超及从这两套拷来的新根走核销。顾客已付待履约显示「待核销」。
- 整单一次核销，不能按件数核一部分。本期不设核销天数，也没有到期自动退。
- 拼团不另出一张主码。全额退完：原码不能再核，不补第二张主码。
- 非核销员不能核。他店的码不能核。有待处理售后时不能核。

## 图

```mermaid
flowchart TB
  subgraph 顾客["顾客"]
    direction TB
    cStart((顾客打开这一家美发或商超小程序<br/>不是众享源品，也没有桌台主路径))
    cPick["顾客选服务或选商品下单<br/>须已登录；认店只认这一家店"]
    cPay["顾客先付钱"]
    cCode["顾客看见一张有效主码，状态显示待核销<br/>拼团也不另出一张主码"]
    cShow["顾客到店出示这张码"]
    cDone["顾客看见订单已完成<br/>可以写评价，评完仍是已完成，没有待评价主状态"]
    cClosed["订单已关闭<br/>原码作废，没有第二张主码"]
  end

  subgraph 店员小程序["店员小程序"]
    direction TB
    dOpen["店员在同一套小程序核销入口输入或扫码<br/>须有核销员权限；不是另一套独立 App"]
    dWho{"这个人有核销权限吗？"}
    dNoStaff["不能核<br/>权限不落到非核销员"]
    dShop{"这是本店的合法未核码吗？"}
    dOther["他店的码、无效码、已经核过的码：都不能再当未核销成功"]
    dAfter{"这一单有待处理售后吗？"}
    dBlock["不能核<br/>须先办完售后"]
    dOk["整单一次核销成功<br/>码不能再核，订单变成已完成，钱已在该店账户"]
    dPart["无此路径：按件数或次数核一部分"]
    dDays["无此路径：按天数作废码、到期自动退"]
  end

  subgraph 租户后台["租户后台"]
    direction TB
    bOpen["老板在租户后台订单页填码或核这一单<br/>顾客看见的结果仍是这一家店的单变成已完成"]
    bSame["后台核销和店员核销是同一套结果<br/>不要画成两套互不相认的码"]
  end

  subgraph 退款["退款"]
    direction TB
    rFull{"这一单已经全额退完了吗？"}
    rVoid["原码不能再核，不补发第二张主码<br/>订单已关闭"]
  end

  cStart --> cPick --> cPay --> cCode --> rFull
  rFull -->|已全额退完| rVoid --> cClosed
  rFull -->|未退完| cShow
  cShow --> dOpen --> dWho
  dWho -->|没有核销权限| dNoStaff
  dWho -->|有权限| dShop
  dShop -->|他店、无效、已核过| dOther
  dShop -->|本店合法未核码| dAfter
  dAfter -->|有待处理售后| dBlock
  dAfter -->|没有卡住的售后| dOk --> dPart --> dDays --> cDone
  cShow --> bOpen --> bSame
  bSame --> dWho
```

## 不画什么

- 不画餐饮扫桌、在线商城的顾客端发货、核销调用先后时序（同夹时序本批不画）。
- 无此路径：部分核销、按天有效期、过期自动退、补第二张主码、待评价主状态、外卖、第四种履约。
- 不要把核销只画在租户网页上。
- 不画端口和域名。通知文案不验收死句子。
