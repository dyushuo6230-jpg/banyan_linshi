# 视觉修复循环档案

| 字段 | 值 |
|---|---|
| 文档名称 | 视觉修复循环 |
| 版本 | 0.1.0 |
| 状态 | 已跑过首页顶区、首页整页、首页选中切图、分类左栏两态、分类外底板；底栏 32A 用户改回 30rpx |
| 更新时间 | 2026-09-19 |

独立手续，不占 G0～G11，不计文档进度分母。口令见使用说明第 32A 节。Skill：`.cursor/skills/visual-repair-loop/`。

源品分类页：先读 `EFFECTIVE_UI_CONTRACT` 0.1.5，再读 `UI_SPEC.md` 0.1.5，再对 `category.vue`。附近列表同样读序，再对 `index-f.vue`。合同覆盖项不得当 PNG 差异去改代码。

| 路径 | 用途 |
|---|---|
| `templates/` | RUN.yaml、DIFF.md 模板 |
| `LOOP_RUNS/` | 每一次点名循环一档 |

默认视口 `390x844`。第一档为固定视口截图对照。未点名不得改页面。
