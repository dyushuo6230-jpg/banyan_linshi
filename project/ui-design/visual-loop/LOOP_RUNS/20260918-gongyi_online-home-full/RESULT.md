# RESULT

- 运行：20260918-gongyi_online-home-full
- 结论：**通过**
- 圈次：2 / 最大 4
- 时机：静态
- 令牌：`docs/project/ui-design/products/gongyi_online/materials/tokens/design-tokens.json`（已确认）
- 不得写成「已按批准 UI_SPEC 验收顶区结构」：顶区听口令红框覆盖，覆盖了 UI_SPEC/定稿的金顶+独立 KV。

## 做了什么

`home.vue` 公告以下：分类选中改 `brand-gold` + `text-primary`；618 价签白底 + `price`；推荐现价 `display` 40rpx；主图上沿 `lg` 圆角；未选中细边 `#E8E8E8`；专区页脚去掉重复标题。顶区保持上一档红框覆盖，未还原金顶+独立 KV。未改分类页、底栏、附近。

## 剩余

D6/D7 按需求与 UI-DEC-002 不修。D10 缺券数据，静态不失败。
