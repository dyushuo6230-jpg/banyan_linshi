# WORKLOG-20260915-003

| 字段 | 值 |
|---|---|
| 文档名称 | DEV-013 本机 SQL 34 与自测 |
| 文档编号 | WORKLOG-20260915-003 |
| 版本 | 0.0.0 |
| 状态 | Draft |
| 创建日期 | 2026-09-15 |
| 更新时间 | 2026-09-15 |

- 日期 / 人员 / 版本 / 批次：2026-09-15 / TBD / 文档 v0.1.41 / DEV-013
- 阶段：IMPLEMENTATION（In Progress）
- 本次目标：本机 shop 库执行 `34-mall-campaign-section.sql`，按 DEV-013 测试行自测。不标 Baselined；本轮仍不真扣款；不走第 36A 节；不提交除非用户另要

## SQL 34

本机 docker `mysql2024`、库 `shop` 已执行（可重复）。核对：

| 项 | 结果 |
|---|---|
| 表 | `mall_campaign_session` / `apply` / `team`；`platform_section` / `_goods` / `_item` |
| 首页模块归属 | `groupon`、`seckill` 已有 |
| 种子板块 | `superbrand` 超级品牌 |
| 平台菜单 | 商城秒杀 / 秒杀报名 / 商城拼团 / 拼团报名 / 板块列表 / 板块内容管理 |
| 租户在线商城菜单 | 秒杀报名 / 拼团报名 / 板块报名 |
| 未恢复 | 无「秒杀管理」「拼团审核」行 |

测试站若尚未打该 SQL，后台菜单与表仍不可见。未打生产。

## 自测环境

- mall-online **8035**；admin 8031；merchant 8033；mall 8032
- 源品 H5 Vite **5176**，代理 `/api`、`/uploads` 指向本机 8035（离开测试站域名）
- 平台/租户 Vite 5173/5174 绑 `[::1]`，须用 `localhost` 不是 `127.0.0.1`
- 本地生活 H5 5175 无 appid 时店铺未加载，坑页未在浏览器出画面

## 测试行

| 测试行 | 结论 | 证据 |
|---|---|---|
| 加购进购物车并可结算未付 | **过** | 详情有加入购物车；API 购物车结算未付单 26（type=1, status=10） |
| 入驻入口点敬请期待 | **过** | 「我的」仍有商家入驻；点了 toast，不进半成品入驻页 |
| 底栏四 Tab；发现无 | **过** | 购物 / 附近 / AI助手 / 我的 |
| AI 假页 | **过** | AI 助手页可进，不做真对话 |
| 商城活动可进未付、未成团关未付单 | **过** | 秒杀未付 27 type=4；拼团未付 28 type=5 后关成 60，占用 0 |
| 板块按标识取数 | **过** | `/campaign/section?code=superbrand` 出联调-优选大米 |
| 收藏足迹 | **接口过 / 页要登录** | POST 接受 `goods_id`；GET 默认 type=1；PUT 只取消。C 端收藏页未登录进账密登录 |
| CPS 两行 | **画面有、未接目录轮播** | 美团外卖～更多两行仍在；点了敬请期待。接口 nearby-cps 有数 |
| 分类二级三级 | **未过** | `indexPage/category` 仍是静态稿（耶稣丸子等），未接 `GET /categories` |
| 附近点店/点货两套详情 | **点店过 / 点货未过** | 点店进 `details-f`（花里云食自助烤肉）。附近列表无货卡，点货未实证。超市便利等静态坑仍在（口径要删） |
| 本地首页拼团秒杀坑 | **接口过 / H5 未出画面** | 归属表 + 装修开关有；5175 无店铺信息 |
| 有 PNG 页一比一、无图不卡像素 | **未整页对稿** | 本窗未逐张 PNG 像素验收 |
| 真扣款 | **不做** | `pay/jsapi` 返回平台未开通支付，符合 DEC-050 |

平台/租户后台：菜单已入库；本窗只到登录页（验证码内存、未走登录 UI）。办理链用签发票走接口。

## 拆桩 / 留下

临时桩：签发 JWT、改 `must_change_pwd=0`、上架货 12/13/14、灌场次报名——灌库与测试数据，不算改口径；JWT 签发未进产品代码。

留下的口径修复（相对已确认规则，不进第 36A 节）：

- mall-online 收藏：C 端 `goods_id`、默认商品类型、取消走 Remove 不再 Toggle 反加
- 源品 Vite 开发代理改回本机 8035
- 「我的」入驻 toast；收藏/足迹进现页
- 首页「限时折扣」进秒杀列表（不再进超级品牌）；「更多」进分类页（不再敬请期待）

## 未完成

- 分类接平台类目二级三级
- 附近去静态坑、列表接本地生活租户、点货进电商详情；CPS 两行接平台目录轮播
- 本地生活 H5 有店时验证拼团秒杀坑画面
- 平台/租户后台菜单页登录后点开
- 未合 main；开发执行进度仍 0%
- 本批结束前须走第 36A 节；自测未整行通过，**不得**把本窗写成验收通过

## 修改文件

本日志；工作日志目录；看板下一步。业务：mall-online 收藏；源品 `vite.config.ts`、`mine/index-w.vue`、`home.vue` 快捷/更多。

## Commit / PR

无

## 对应需求 / 设计 / UI Contract

REQ-060、REQ-063～065、REQ-131～133、REQ-043、REQ-054。ADR-011 v1.1、ADR-016 v1.1、DEC-068、DEC-069、DEC-050。UI_CONSTRAINT_MODE=NONE。

## 阻塞和下一步

无 P0/P1。下一批次不排。先补分类与附近口径缺口，再复测未过行。
