# WORKLOG-20260907-003

| 字段 | 值 |
|---|---|
| 文档名称 | DEC-046 切片1：新增本地业态 |
| 文档编号 | WORKLOG-20260907-003 |
| 版本 | 0.0.0 |
| 状态 | Draft |
| 创建日期 | 2026-09-07 |
| 更新时间 | 2026-09-07 |

- 日期 / 人员 / 版本 / 批次：2026-09-07 / AI / 文档 v0.1.21 / DEC-046 切片1
- 阶段：IMPLEMENTATION
- 本次目标：经营类目可增本地根、克隆商户菜单、业态根入驻可选；新根本默认关

## 已完成

- 知情决策二次确认已 APPLY：LOCAL-ADD-WHEN=A、LOCAL-HOME-MAP=B、LOCAL-ADMIN-MENU=A、LOCAL-CPS-DEFAULT=A、LOCAL-APPLY-GATE=A
- 增量 SQL `14-local-business-type.sql`：`merchant_category.apply_open` / `clone_from`；原四根入驻开；`POST /api/v1/business-tree/local-roots` 授权超管
- 新增本地根：必选已有本地业态拷贝菜单（禁止在线商城）；GORM 零值不会把 `apply_open=0` 吃成默认 1；树接口吐出新增根（不再只吐冻结四根）
- 平台经营类目：新增本地业态、入驻列/开关；商户菜单页签跟树走；入驻审核补选全部根；H5 只列 `apply_open` 的根
- 入驻提交校验开放根；审核补选只要求根存在+有菜单

## 未完成

- 切片2 首页模块归属、切片3 周边/CPS 未开工
- 未标 Code Complete；开发进度仍 0%
- 未合入
- 入驻 H5 未在本机 uni 跑通（只改了源码）

## 修改文件

`pkg/businesstree`、经营类目/菜单/审店前端、入驻 H5、`14-local-business-type.sql`、本日志。

## Commit / PR

无

## 测试结果

- `go test ./pkg/businesstree/` 通过
- `go build` admin / mall / merchant 通过
- 本机已执行 SQL 14
- 平台经营类目已能新增本地根；菜单出现对应页签；新根入驻为关。本机验证时留了一条 `wash`（洗车，拷贝餐饮，入驻关）便于联调

## 文档同步

DEC-046 / CR-021 已在此前 APPLY。本批不改 `admin_menu`（归属/CPS 在切片2/3）。

## 交接

下一刀切片2：商户管理下独立「首页模块归属」。不创建 `app/mall-online`。不发明第四种履约。
