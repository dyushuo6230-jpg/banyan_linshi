# WORKLOG-20260907-011 交易结算

| 字段 | 值 |
|---|---|
| 日期 | 2026-09-08 |
| 批次 | DEV-009 |
| 合入 | 无 |
| 状态 | Ready（未合入） |

## 做了什么

- 用户书面「开工」。未合入。进度仍 0%。
- 新单抽佣改读业态根/一级类目当前费率快照，不再用店铺 `commission_rate`。已付单不重算。未配置费率不当成 0%。
- 本地生活支付成功不写可提现冻结；财务确认入账只改结算单状态（已收），不增加可提现。在线商城仍可入账提现。结算筛选不以 `is_self` 为准。
- 本地生活 JSAPI/退款用该店 `merchant_pay_config`；在线商城用 mall 配置里的平台商户号，未配则「平台未开通支付」，不编造、不回落到店号。
- 在线商城未挂可计佣一级：不可售、不可发、支付快照拒绝。
- 未编造对公账号/账期。不创建 `app/mall-online`。不改结算表结构、不改账单显示名。
- 增量 SQL `nunu-go-api/deploy/sql/20-trade-snapshot.sql`：`order_info.commission_rate`。

## 测试结果

- `go test ./pkg/trade/` 通过（根费率不是店费率；NULL 不当 0%；克隆根用自己的费率；在线商城用一级费率；未配一级拒绝；本地生活结算单=应付佣金、在线商城=货款−退−佣；已付单不因改树而变）
- `go test ./pkg/goodssellable/` 通过（未配一级不可买、不可发）
- `go test ./pkg/fulfillment/` `./pkg/groupseckill/` 仍通过
- 平台确认入账单测：本地生活余额不变且状态=已收；在线商城可入账
- 支付通道单测：本地生活用店商户号；在线商城忽略店号，平台未配则拒绝
- `go build` mall / merchant / admin 通过
- 公开接口：支付方式列表未登录可访问；JSAPI 未登录 40101；平台确认入账未登录 40101
- HTTP + 库流程模拟（临时脚本，测完已删，种子已清零）：
  - 本地生活快照不读店铺 `commission_rate`，未配根费率不当成 0%
  - 本地生活确认入账后可提现余额不变
  - 在线商城快照用一级费率不是店费率
  - 在线商城下单支付不回落到「店铺未开通支付」（走平台通道；平台号已配则不编造新号）
- 未测：真微信支付、真机
- 2026-09-08 用户书面 `R8-1=A` `R8-2=A` `R8-3=A`（DEC-048）。标 **Ready**。未合入。
