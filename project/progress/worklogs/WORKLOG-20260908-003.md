# WORKLOG-20260908-003 DEV-010 众享源品 - 在线商城 mall-online 自测

| 字段 | 值 |
|---|---|
| 日期 | 2026-09-08 |
| 批次 | DEV-010 |
| 合入 | 无 |

## 做了什么

- 创建独立进程 `nunu-go-api/app/mall-online`（mall 模板复制，本机 **8035**）。JWT Redis 前缀 `shop:mall-online:*`，只服务 `user_pool=2`。
- 去掉扫桌/认店/店账户支付/本地生活拼团秒杀/入驻/小魔推等路由。首页读平台 `banner`，附近=当前全部本地根，CPS=平台上架目录且 `extraData.from=mall-online`。
- 密码/短信登录进池 2；众享营销 - 本地生活 mall 不开这套密码登录。下单写池 2；支付走本进程平台商户号配置（空则「平台未开通支付」，不编造）。
- `go-uni-app-online` 登录页接真接口（不改视觉）；请求成功码改为 0，开发代理仍 8035。

## 自测

- `go build ./app/mall-online/cmd/server` 通过。
- 单测：`pkg/userpool`、`pkg/nearbycps`、`pkg/goodssellable`、`pkg/trade`、mall-online repository/service/middleware 通过。
- HTTP（本机 8035，无 appid）：`GET /home`、`/nearby-categories`、`/nearby-cps`、`/goods` 均为 code=0；密码登录失败文案为「账号或密码错误」，不是「缺少当前店铺」。mall 8032 附近仍要 `merchant_id`。
- 未过审商品未出现在众享源品 - 在线商城列表。平台商户号未配，不编造。

## 未做

- jwt.secret / 微信商户号 / 小程序 appid 仍待上线配置。
- 众享源品 - 在线商城页面前端接口按 DEC-029：先做到能打包、够申请支付；真机/合入/上线另做。
- 本期不做 AI/发现、不做在线商城拼团秒杀。
