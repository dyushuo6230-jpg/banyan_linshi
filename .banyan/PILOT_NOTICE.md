# Banyan Project Instance Pilot

This directory is a Stage 17 pilot and shadow projection. It is not canonical truth, does not replace the existing project layout, and does not authorize Git mutation or migration of `docs/` or `project/`.

All `UNKNOWN` and blocked states remain explicit. Runtime policy and the independent Stage 17 gate remain authoritative for actions.
