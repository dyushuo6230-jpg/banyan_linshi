# 通用文档提示词 v3.1 完整使用说明

## 文档信息

- 版本：v3.1.15
- 大版本：v3.1
- 状态：Draft
- 配套主提示词：《通用文档提示词v3.1.md》（v3.1.15）
- 适用场景：新项目、旧项目重构、已有项目功能扩展、后台管理、Web、App、小程序和多终端项目

## 文档迭代说明

### v3.1.15

- 保持 `v3.1` 大版本、目录名称、G0～G11 阶段和第 1～52 节编号不变。不新增 G 阶段。
- G9.5 禁止跳步：令牌确认后下一刀是第 28A 节，不是换皮。第 25A、28A、29、51、52 节写明收工三行。用户预定页只记 Partial。

### v3.1.14

- 保持 `v3.1` 大版本、目录名称、G0～G11 阶段和第 1～52 节编号不变。不新增 G 阶段。新增第 23A、25A、28A、32A 节口令，性质同第 36B、39A：独立手续或补充口令，不是新阶段。
- 正式 UI 规范改为总闸 + 分闸。G9.5 接入 anydesign。允许 Partial。同一套代码多端共用应用级 materials。
- 视觉修复循环为独立手续，随时可点名，不计分母。第一档固定视口截图对照。禁止业务路由 `/v2`。
- 第 2、23、25、28、33、51、52 节交叉引用。细则以主提示词十八、十八之二、二十二之二为准。

### v3.1.13

- 保持 `v3.1` 大版本、目录名称、G0～G11 阶段和第 1～52 节编号不变。不新增 G 阶段。
- 自测验收：不得为了过验收改口径。临时桩、绕过、写死、假装成功，测完必须拆掉。测出产品偏离已确认口径，修掉并留下；要改规则先走第 2A 节 / CR。灌库和改测试数据不算改口径。拆桩不是第 36A 节的 PRD 变更。
- 写入第 1、35、36 节；第 36A 节只排除「拆桩≠PRD 变更」；第 48、50、51、52 节交叉引用。细则以主提示词核心原则 25 为准。

### v3.1.12

- 保持 `v3.1` 大版本、目录名称、G0～G11 阶段和第 1～52 节编号不变。不新增 G 阶段。
- 在引用保护：仍被其它有效正式文档按原主题引用的规则正文，禁止删除、清空或整篇换题。同主题修订升版本；新主题新号新文件。写入前先检索引用。
- 第 39C、40～42、51 节交叉引用。细则以主提示词「四、在引用规则保护」为准。

### v3.1.11

- 保持 `v3.1` 大版本、目录名称、G0～G11 阶段和第 1～52 节编号不变。不新增 G 阶段。
- 编号占用：已占用的 DEC / ADR / CR / UI-DEC / REQ 号禁止改挂另一主题。新主题按当时最大号 +1。若发现整篇换题，不为还原旧号再挪走已纳入的新主题；从 git 恢复被覆盖正文到最大号 +1；按语义分类改引用，禁止全局替换。先出对照表再写入。
- 第 39C、40～42、51 节交叉引用。细则以主提示词「四、编号占用规则」为准。

### v3.1.10

- 保持 `v3.1` 大版本、目录名称、G0～G11 阶段和第 1～52 节编号不变。不新增 G 阶段。
- 第 36C、36D 节增加对外白话架构图密度：流程图和泳道每个框必须是完整业务话（谁、对象/订单变成什么、钱或权限落到哪、本期没有的路标「无此路径」）。禁止空标题框。时序用人话、不写接口参数。静态图框和箭头必须有人话含义。
- 第 1、51、52 节交叉引用。

### v3.1.9

- 保持 `v3.1` 大版本、目录名称、G0～G11 阶段和第 1～52 节编号不变。不新增 G 阶段。不把对外白话写成第 53 节。
- 新增第 36B～36D 节：可选对外白话。三份口令分开：第 36B 节只建骨架；第 36C 节初次创建按清单写正文；第 36D 节差异同步只刷过期篇。施工细则以 `{{DOC_ROOT}}/plain-spec/白话文档生成方案.md` 为准，不要另发明口令。
- 空项目初始化（G0 / 第 3 节）不生成白话正文、不创建对照表。批次结束先走第 36A 节，再按第 36D 节刷差异。
- 第 1、1A、2、3、35、36A、48、50、51、52 节增加交叉引用。

### v3.1.8

- 保持 `v3.1` 大版本、目录名称、G0～G11 阶段和第 1～52 节编号不变。不新增章节、不新增 G 阶段、不单独建「人话范式」文件。
- 第 2A 节 `INFORMED_REVIEW`：涉及功能时，决策背景（或紧挨着的「功能在哪看到」）强制套人话 6 问；缺一块不得提问。生动例子必须能按 6 问走一遍。这里写全与主提示词二-A 同一套必写块。
- 第 1A、第 39B 节可复制口令各加一句「功能描述按第 2A 节人话 6 问」，不把 6 问全文再贴进口令。
- 第 52 节交叉引用。只约束功能，不约束表结构、接口路径、SQL 编号、部署。

### v3.1.7

- 保持 `v3.1` 大版本、目录名称、G0～G11 阶段和第 1～52 节编号不变。
- 新增第 39A～39C 节：可选「变更稿」能力。双人同分支时，需求先写入不入库的临时袋，再迁入正式文档。
- 第 39A 节只铺说明书和忽略规则，每个文档根目录执行一次即可；不塞进空项目初始化（G0）。
- 第 39B 节进入后只写临时袋；第 39C 节纳入时按当前 PRD 状态走第 40～42 节。未铺路过就进入时，先做第 39A 节再进入。
- 变更稿不是新的 G 阶段，不得改写 `PROJECT_STAGE`。第 1A、3、35、36A、37、38、48、51 节增加交叉引用。

### v3.1.6

- 保持 `v3.1` 大版本、目录名称、G0～G11 阶段和第 1～52 节编号不变。
- 第 2A 节强制：知情决策只在当前会话窗口用正文提问，禁止弹框、问卷卡、结构化选项表、原生确认卡片。
- `INFORMED_REVIEW` 必写四块加推荐：决策背景与问题描述、备选方案对比、推荐选择及核心理由、开发技术决策清单；必须有生动形象的解释。关闭弹框或只点选项不构成初选/确认。

### v3.1.5

- 保持 `v3.1` 大版本、目录名称、G0～G11 阶段和第 1～52 节编号不变。
- 新增第 36A 节：每个开发批次结束、进入下一批次前，盘点本批已确认的逐条业务改动；有则起草一张 CR 并收入 PRD，无则记录后继续。
- 第 35、36、48、50、51 节增加该批次门禁；CR 写入仍走第 40～42 节和第 2A 节，本节不替代变更流程。
- 纯实现不写入 PRD。已 Approved/Baselined 的 PRD 以补丁版本叠加，不覆盖旧基线。

### v3.1.4

- 保持 `v3.1` 大版本、目录名称、G0～G11 阶段和第 1～52 节编号不变。
- 新增第 1A 节：新对话窗口没有聊天记忆，用项目文档进度续做；提供可复制提示词。

### v3.1.3

- 保持 `v3.1` 大版本、目录名称、G0～G11 阶段和第 1～52 节编号不变。
- 新增第 2A 节全局知情决策协议，适用于冲突处理、候选需求、参与模式、G6/G7、UI 问答、CR、审批前决策和状态变更。
- 所有决策问答统一为 `INFORMED_REVIEW → INFORMED_CONFIRMATION → DECISION_APPLY`，初次选择不得直接形成 DEC、ADR、CR、UI-DEC 或修改状态。
- 高影响决策每轮只处理 1 项且不与其它决策混排；低影响决策每轮最多 2 项。所有方案必须先解释实际运行、数据与依赖、事务、优缺点、风险、成本、性能、交付、迁移、测试、发布、回滚和安全隐私影响。
- 用户表示不理解、要求继续解释、取消问卷、跳过或只表达倾向时，不构成确认；AI 必须继续解释或保持 `TBD`。
- 第 9、12、15、18、19、22、24、27～32、40～52 节及相关检查清单统一引用第 2A 节，消除“用户初答后立即写入”的旧行为。

### v3.1.2

- 保持 `v3.1` 大版本、目录名称、G0～G11 阶段和原有章节编号不变。
- 第 18 节增加架构参与模式选择、门禁预检、只读决策评审、用户确认、决策应用、Draft 生成和误生成恢复的完整说明。
- 安全默认值改为 `ARCHITECTURE_PARTICIPATION_MODE=MANDATORY`；单条生成口令不能自动跳过决策评审。
- 增加 `ARCHITECTURE_DECISION_REVIEW_STATUS`、`ARCHITECTURE_DECISION_APPLY_STATUS` 和 `ALLOW_ARCHITECTURE_DRAFT_WITH_TBD`。
- 保留直接生成 Draft 的能力，但必须显式设置模式和授权，且未决项继续为 `TBD`。
- 第 19 节默认继承第 18 节参与模式，并增加同类模块边界门禁，防止再次跳过用户确认。
- 增加已提前生成 Draft 的恢复流程：保留现有 Draft，先补决策，再同步文档。

### v3.1.1

- 保持 `v3.1` 大版本、目录名称、G0～G11 阶段和原有章节编号不变。
- 在第 18 节和第 19 节中增加可选深度参与方式，不改变原有直接生成 Draft 的提示词。
- 第 18 节可在生成总体架构前执行 `ARCHITECTURE_DECISION_REVIEW`。
- 第 19 节可在生成 MODULE_BOUNDARIES 前执行 `MODULE_BOUNDARY_DECISION_REVIEW`。
- 两种评审均限制为每轮最多 2 项，先展示方案、优缺点、推荐理由和影响，再由用户确认。
- 用户确认前不修改正式技术文档，未确认内容保持 `TBD`。

## 1. 使用原则

v3.1 由两个部分组成：

1. 主提示词：建立长期有效的项目文档规则、目录、阶段门禁和追踪体系。
2. 阶段提示词：在具体阶段告诉 AI 当前输入、允许动作和预期输出。

推荐使用顺序：

1. 首先发送《通用文档提示词v3.1.md》中的完整主提示词。
2. 然后根据当前项目阶段，从本文选择对应的阶段提示词。
3. 每次只执行一个明确阶段或一个开发批次。
4. 不要同时发送互相冲突的阶段提示词。
5. 提示词中的 `{{变量}}` 必须替换为真实值；未知值保留 `TBD`。
6. 如果 AI 当前对话已经加载并遵循主提示词，可以只发送对应的阶段提示词。
7. 第 18～19 节的安全默认模式为 `MANDATORY`：先评审、确认和应用决策，再生成 Draft。
8. 如需直接生成带 TBD 的 Draft，必须显式启用 `DIRECT_DRAFT` 和对应 `ALLOW_*_WITH_TBD=true`，不能只发送生成口令。
9. Cursor 的 Plan/Agent 交互模式与本文的架构参与模式是两套机制；是否切换 Cursor 模式不代表用户放弃架构决策权。
10. 所有需要用户选择、裁决、批准或授权状态变化的提示词，均须按第 2A 节知情决策协议执行。知情决策只写在会话正文，禁止弹框/问卷卡。
11. 会话中的初次文字选择只代表候选意向；AI 完成影响复述且用户二次确认后，才能进入写入步骤。关闭弹框、只点一次选项不构成初选或确认。
12. 新对话窗口没有上一窗聊天记忆。续做时发送第 1A 节提示词，以 `{{DOC_ROOT}}` 下看板、待确认问题和已应用 DEC 为准，不要另建口头语流水账。
13. 每个开发批次结束时，若本批有已确认的逐条业务改动尚未写入 PRD，必须先按第 36A 节起草 CR 并收入 PRD，再开始下一批次。
14. 对外白话（如启用）不是新阶段。第零批发第 36B 节，初次创建发第 36C 节，差异同步发第 36D 节。没有对照表不得写正文。G0 不生成白话正文。施工细则以 `{{DOC_ROOT}}/plain-spec/白话文档生成方案.md` 为准。架构图（流程图、泳道）必须按完整业务话密度画，禁止空标题框。
15. 已占用的 DEC / ADR / CR / UI-DEC / REQ 号禁止改挂另一主题。发现整篇换题时按主提示词「编号占用规则」处理，先出对照表，用户确认后再写入。
16. 仍被其它有效正式文档按原主题引用的规则正文，禁止删除、清空或整篇换题。写入前先检索引用。同主题修订升版本；新主题新号新文件。
17. 自测不得为了过验收改口径。临时桩测完必须拆掉。测出产品偏离已确认口径则修掉并留下；要改规则先走第 2A 节。细则见第 35、36 节。不塞进第 36A 节当 PRD 变更。
18. 视觉修复循环（第 32A 节）不是 G 阶段，可在静态或验收时点名启动，不挡功能开发，不计文档进度分母。anydesign（第 25A 节）只产素材，不能直接当已批准 UI_SPEC。令牌确认后下一刀是第 28A 节，禁止把点名换皮说成下一步。G9.5 每步收工必须报当前节、下一节口令、禁止说成什么。

## 1A. 新窗口继续工作

### 提示词使用说明

关闭当前对话、新开窗口、换模型或隔一段时间再继续时使用。AI 不会自动记得上一窗内容。进度真源是 `{{DOC_ROOT}}` 里已写入的看板、待确认问题、已应用 DEC/ADR/CR 和工作日志，不是聊天记录。

不要为续聊单独维护「口头语」文档。未二次确认的口头约定不得写成 DEC。

每次只续做文档标明的**下一步一项**（高影响每轮 1 项）。不要重做 G0，不要重放已冻结资料，不要在 P0/P1 未清时生成 PRD。

```text
请完整加载并严格遵循：
@docs/governance/common_prd_v3.1/通用文档提示词v3.1.md
中的「主提示词」全文，以及
@docs/governance/common_prd_v3.1/通用文档提示词v3.1完整使用说明.md
中的阶段门禁和第 2A 节知情决策协议。
功能描述按第 2A 节人话 6 问。

这是新对话窗口续做，不是新项目初始化。

文档根目录：{{DOC_ROOT，默认 docs/project/}}
进度数据真源：{{DOC_ROOT}}/progress/PROGRESS_REGISTER.yaml
人类可读进度：{{DOC_ROOT}}/progress/PROJECT_DASHBOARD.md
待确认问题：{{DOC_ROOT}}/baseline/05_PENDING_QUESTIONS.md
已应用决策：{{DOC_ROOT}}/baseline/DEC-*.md（以及 decisions/ 下已批准项）

请先只读上述文件和项目变量 {{DOC_ROOT}}/PROJECT_VARIABLES.md，然后：

1. 用一两句话汇报：当前 PROJECT_STAGE、已应用 DEC 编号、剩余 P0/P1、文档标明的下一步。
2. 只执行该「下一步」一项。高影响走 INFORMED_REVIEW，只读，不写 DEC，不生成 PRD。
3. 不要重做 G0/G1，不要重写已冻结来源，不要作废已应用 DEC。
4. 不要根据「可能聊过」的内容补写口头结论；只采用已写入文档的结论。
5. ALLOW_CODE_CHANGES 以项目变量为准；文档阶段不得改业务代码。

若看板上一步是知情评审，直接进入该项 INFORMED_REVIEW。
若看板上一步是用户已选方案待二次确认，停留在 INFORMED_CONFIRMATION，不要 APPLY。
若看板上一步是已确认待应用，且用户本条已给出「确认该决策」口令，才进入 DECISION_APPLY。

若存在 {{DOC_ROOT}}/changes_temp/BAG.md：先用一两句话汇报临时袋状态（开着与否、未纳入条数）。袋内草稿不得当成已应用的 DEC/CR。
若存在 {{DOC_ROOT}}/progress/PLAIN_SYNC.yaml：用一两句话汇报对照表是否有 stale、缺口文件是否有未闭环项。续做白话时发第 36B / 36C / 36D 节对应口令，不要整夹重生。
```


## 2. v3.1 阶段总览

```text
G0    文档初始化
G1    资料投放
G2    来源和决策基线
G3    PRD 准入审计
G4    PRD Draft
G5    PRD 评审和批准
G5.5  根据 PRD 同步 UI 逻辑范围
G6    总体技术架构
G7    技术模块划分
G8    模块详细技术设计
G9    API、数据和编码规范
G9.1  根据应用架构和项目结构生成正式 UI 应用目录
G9.5  可选 UI 设计规范
G10   开发批次和有效 UI Contract
G11   开发、测试、发布和维护
```

其中：

- `G5.5` 默认执行，只建立 UI 逻辑范围，不确定真实应用目录名。
- `G6` 默认强制完成决策评审和应用后再生成 Draft；显式直接生成受 v3.1.3 模式变量和知情决策协议控制。
- `G7` 默认继承 G6 参与模式，模块拆分争议未确认时不能静默生成 MODULE_BOUNDARIES。
- `G9.1` 根据已确认的应用架构和 `PROJECT_STRUCTURE.md` 生成正式应用目录。
- `G9.5` 是可选阶段。总闸关闭，或分闸名单未包含当前应用时跳过。
- 不投放 UI 素材、不生成 UI_SPEC，也可以继续开发。
- 一旦某个已分闸应用的批次声明使用 UI 规范，该批次必须使用合并后的 `EFFECTIVE_UI_CONTRACT.md`。
- G2、G5、G6、G7、G9.5、CR 和 UI 冲突中的所有决策问答均遵循第 2A 节。
- 变更稿（第 39A～39C 节）是可选会话模式，不是新的 G 阶段，不得改写 `PROJECT_STAGE`。一人做到底可不用。
- 对外白话（第 36B～36D 节）是可选会话能力，不是新的 G 阶段，不得改写 `PROJECT_STAGE`。G0 不生成白话正文。没有对照表不得走初次创建或差异同步。
- 视觉修复循环（第 32A 节）是可选独立手续，不是新的 G 阶段，不得改写 `PROJECT_STAGE`。未点名不得执行。

## 2A. 知情决策全局协议

本协议简称 `IDP`。

### 适用范围

凡需要用户选择方案、裁决冲突、确认候选、批准版本、接受风险或授权状态变化，
均必须执行本节。包括但不限于：

- P0/P1 冲突和候选需求；
- 架构参与模式、G6 架构决策和 G7 模块边界；
- UI 规范、素材替代、高保真等级和 UI-CONFLICT；
- CR、DEC、ADR、UI-DEC、Approved、Baselined 和阶段门禁。

纯事实收集问题不要求提供多个方案，但 AI 必须先说明为什么需要该信息、
接受什么格式、未知时是否可以保留 `TBD`。事实回答会改变业务结论或状态时，
仍须转入完整知情决策流程。

凡描述功能（知情决策、CR、对 bug 说话）：人能碰到的行为，或系统办完后人在某页能看见的结果，
必须按本节人话 6 问写完。不约束表结构、接口路径、SQL 编号、部署。

推荐项目变量：

```text
INFORMED_DECISION_MODE=MANDATORY
```

`SIMPLIFIED` 仅可由用户明确启用并用于低影响决策；高影响决策不能简化。
本协议没有 `OFF` 模式。

### 影响分级和每轮数量

高影响决策包括：

- 金额、支付、退款、分账、结算；
- 账号、身份、权限、数据范围、隐私和安全；
- 状态机、数据归属、删除、迁移和不兼容变更；
- PRD 业务规则、总体架构、模块边界；
- Approved/Baselined、跨阶段门禁和生产风险。

高影响决策每轮只允许 1 项。低影响、可逆且不改变业务语义的决策每轮最多 2 项。
不得在同一轮混合高影响和其它决策。
同轮包含 2 个低影响决策时，必须为每项分配独立决策 ID，
分别提问、分别复述、分别二次确认，并在 APPLY 中逐项列明；
不得用一次合并确认替代两个独立确认。

### 阶段一：INFORMED_REVIEW

知情决策必须写在**当前会话窗口的正文消息**里。禁止弹框、问卷、结构化选项表、原生确认卡片、IDE 表单或把选项收成「点一下」的控件。关闭弹框、取消表单、只点一次选项，不构成初选或确认。

在本会话提出选择问题前，AI 必须按下列结构写完，缺一块不得提问：

1. **决策背景与问题描述**：决定什么；当前卡在哪；不决定会阻塞什么；相关 PRD、来源、DEC、ADR、现码事实和治理门禁。
   若本决策涉及「功能」（人能碰到的行为，或系统办完后人在某页能看见的结果），本块必须含紧挨着的「功能在哪看到」，并强制套下面人话 6 问；缺一问不得提问。禁止只用字段名、文件名当背景。表结构、接口路径、SQL 编号、部署不受此约束。

   **功能在哪看到 / 人话 6 问**（知情决策、CR、对 bug 说话，凡描述功能都用这一套）：

   1. 哪一端、哪一业态：平台后台 / 商家后台 / 店小程序 / 共益优选 / 入驻 H5。多端分行。四业态不同就分行。禁止只写「系统里」「后台」。
   2. 谁，或谁也不用点：人写身份；机器写「不用人点，支付成功后 / 每天跑批后」。
   3. 怎么走进来：后台写侧栏「一级 → 二级 → 页签」（现码菜单名）；小程序写底部或「我的 / 订单」；H5 写从哪页哪个按钮进来。没有入口写「现在无处可进」或「侧栏没有但还能打开某页」。名称吃不准就写页面标题并标明待核对。
   4. 人在哪看见：列表哪一列、详情哪一段、哪个按钮、哪句文案、导出文件哪一列、哪条短信。没有界面就写「过程看不见，结果在【哪一端哪一页哪一块】能看见」。
   5. 怎么操作，或怎么触发：从打开到看见的步骤；要哪类单据。自动任务写什么事件触发。删功能写现在怎么点还能看见它。无权限、没数据时分别怎样。
   6. 现在怎样 / 改完怎样：只写眼睛（或短信、导出文件）看到的变化。删除：现在能看见什么，改完这里不该再有。字段名必须出现时放括号里。

   短句模板：
   【哪一端·哪业态】里，【谁 / 不用人点】，从【怎么走进来】到【人在哪看见】。【怎么操作或触发】之后：现在是……；改完是……。
2. **生动形象的解释**：白话讲清术语；高影响项至少给一个能对照真实用户/商家/平台的生活化例子或完整运行流程。生动例子必须能按人话 6 问走一遍；禁止只用字段名、文件名当背景。
3. **备选方案对比**：2～3 个真实可行方案（推荐方案放第一项，不得包装成已确认）。每个方案按适用范围说明：
   - 实际运行方式以及谁调用谁；
   - 数据、状态和权威写入方归谁；
   - 模块、系统、第三方和基础设施依赖；
   - 事务、一致性、幂等、补偿和失败结果；
   - 具体优点、缺点和主要风险；
   - 开发成本、认知复杂度、运维复杂度和性能影响；
   - 交付、兼容、迁移和现有数据影响；
   - 测试、发布、灰度、降级和回滚方式；
   - 安全、隐私、审计和合规影响；
   - 适用条件和不适用条件。
4. **推荐选择与核心理由**：写明推荐哪一案、为什么推荐、为什么不选另外几案。不得用「推荐」二字代替理由。
5. **开发技术决策清单**：若选各案（至少覆盖推荐案），编码要改什么、不改什么、在哪个批次改、本窗是否允许改代码；待配置不得编造。

确实不适用的维度标记 `N/A` 并说明原因。不得仅给出短选项名称。

每个决策说明完成后只提出一个明确问题，请用户**在本会话用文字**回复方案字母或 `决策ID=方案`；同一轮包含两个低影响决策时，分成两个编号问题，不得合并成一个含糊问题。用户可以选择方案，也可以回复：

```text
继续解释
换个生活化例子
重点比较方案 A 和方案 B
暂不决定
```

用户要求解释时，AI 只补充当前决策，不推进下一项、不修改文件。

### 阶段二：INFORMED_CONFIRMATION

用户第一次选择只是候选意向。AI 必须先只读复述：

1. 决策 ID 和主题；
2. 选择的方案；
3. 用户增加的限制条件；
4. 被拒绝的方案；
5. 对业务、数据、架构、UI、安全、成本、交付、迁移、测试、
   发布和回滚的影响；
6. 继续为 `TBD` 或延期的事项。

然后只提出一个二次确认问题。推荐确认格式：

```text
确认该决策：
{{DECISION_ID}}={{SELECTED_OPTION}}
限制条件：{{CONSTRAINTS 或 NONE}}
```

以下情况不构成确认：

- “倾向于”“可以考虑”“差不多”“先这样”“你决定吧”；
- 用户表示不理解、要求继续说明或要求换例子；
- 取消问卷、关闭弹框、跳过、暂停、沉默、撤回选择或切换话题；
- 只点击一次选项或使用结构化表单点选，但 AI 尚未在会话正文复述影响；
- AI 根据推荐项或历史偏好自行推断。

发生上述情况时，当前项保持候选或 `TBD`，不得写入 DEC、ADR、CR、
UI-DEC，不得提高评审、批准或阶段状态。

### 阶段三：DECISION_APPLY

只有完成二次确认且用户明确授权应用后，才能执行写入：

1. 按类型写入 DEC、ADR、CR、UI-DEC 或受控决策登记。
2. 只应用已经解释和确认的范围，不扩大选择含义。
3. 发现新权衡时停止应用，回到 `INFORMED_REVIEW`。
4. 部分应用标记 `PARTIAL`。
5. 全部必选项完成确认和应用后，才能标记相关状态 `COMPLETED`。
6. APPLY 阶段不继续提出新的决策问题。

业务规则使用 DEC，技术和架构决策使用 ADR，纯视觉决策使用 UI-DEC，
业务行为变化使用 CR；不得用错误记录类型绕过门禁。

### 标准顺序

```text
DISCOVERY
→ INFORMED_REVIEW
→ INFORMED_CONFIRMATION
→ DECISION_APPLY
```

前两个阶段默认只读，不修改正式文档、项目变量和进度状态。
如果用户直接发出明确批准口令，只有在该版本、范围和影响已经完成评审及复述时，
才可视为二次确认和应用授权；否则先补充知情决策说明。

# 第一部分：项目初始化和资料投放

## 3. 初始化未知项目

### 提示词使用说明

项目类型、用户、平台和技术栈尚未确定时使用。它只允许 AI 创建通用文档结构，不会生成虚假的 PRD、架构或 UI 方案。

```text
当前还不知道最终项目类型，也没有开始投放资料。

请按通用项目文档生产规范 v3.1 执行：

PROJECT_STAGE=BOOTSTRAP
REPOSITORY_ROLE=NONE
ALLOW_CODE_CHANGES=false
GENERATE_UI_REFERENCE_TREE=true
GENERATE_UI_APPLICATION_TREE=true
ENABLE_UI_SPEC_GENERATION=false

请初始化：

1. 文档目录和各目录 README。
2. AI_DOCUMENTATION_GUIDE。
3. 资料入口和来源模板。
4. 决策、冲突、问题和变更模板。变更夹 README 写明：要用临时变更袋，先发第 39A 节「初始化变更稿模式」。不要在本次初始化生成《变更稿模式》全文，不要创建 changes_temp。
5. 追踪、进度、工作日志和交接模板。
6. UI 设计根目录入口、逻辑范围和应用映射模板。
7. PROJECT_DASHBOARD。

所有业务、技术和 UI 变量保持 TBD。
文档总进度显示 N/A。
不要生成具体 PRD、架构、技术栈、编码规范和 UI 方案。
不要生成对外白话正文，不要创建 plain-document 下的业务文件和 PLAIN_SYNC.yaml。白话入口见第 36B 节。
不要修改业务代码。
```

## 4. 项目方向明确后更新变量

### 提示词使用说明

已经确定项目类型、目标用户或平台，但仍处于资料投放阶段时使用。只更新项目元信息，不代表业务规则已经确认。

```text
请更新项目变量：

项目名称：{{PROJECT_NAME}}
项目类型：{{PROJECT_TYPE}}
项目目标：{{PROJECT_GOAL 或 TBD}}
目标用户：{{TARGET_USERS}}
目标平台：{{TARGET_PLATFORMS}}
当前项目阶段：INTAKE
当前代码仓库作用：{{NONE/CURRENT_BASELINE/HISTORICAL_REFERENCE}}
默认业务时区：{{BUSINESS_TIMEZONE 或 TBD}}
目标技术栈：{{TECH_STACK 或 TBD}}
允许修改业务代码：false

PRD Approved 后只生成 UI 逻辑范围。
正式 UI 应用目录在应用架构和 PROJECT_STRUCTURE 确认后生成。
当前不生成正式 UI_SPEC。

请只更新项目元信息、README 和项目看板。
不要生成最终 PRD、架构和代码。
```

## 5. 投放历史需求资料

### 提示词使用说明

投放旧版 PRD、旧需求说明、历史会议记录或已经废弃的材料时使用。必须明确它们只是历史参考，避免 AI 把旧行为自动当成新需求。

```text
这是第 {{BATCH_NO}} 批资料。

资料类型：旧版需求
资料路径：{{PATH}}
资料年代：{{DATE 或 UNKNOWN}}
优先级：历史参考
当前阶段：INTAKE

请：

1. 分配来源编号。
2. 更新 SOURCE_REGISTER。
3. 生成批次摘要。
4. 识别新增、修改、保留、废弃和冲突。
5. 更新资料处理进度和项目看板。
6. 将旧资料标记为 HISTORICAL_REFERENCE。
7. 不生成最终 PRD。
8. 不修改业务代码。
```

## 6. 投放设计图用于需求归纳

### 提示词使用说明

设计图当前只用于帮助识别页面、字段、按钮和可见状态时使用。这个阶段的设计图不是正式开发视觉基准。

```text
这是第 {{BATCH_NO}} 批资料。

资料类型：需求阶段设计图
资料路径：{{PATH}}
版本：{{VERSION 或 TBD}}
适用终端：{{ADMIN_PC/MINIAPP/APP/H5/TBD}}
优先级：{{PRIORITY}}

请：

1. 为每张设计图分配 SRC-UI 编号。
2. 登记页面、入口、字段、按钮、可见状态和明确文案。
3. 记录可见布局、颜色、字体、组件和图标。
4. 区分画面事实、AI 推断和待确认内容。
5. 检查与文字需求的冲突。
6. 更新来源登记和资料进度。

这些设计图当前只用于需求归纳，不作为正式开发验收基准。
不要从静态图推断完整权限、状态机、后端流程和异常流程。
不要生成最终 PRD。
```

## 7. 投放新版需求和技术参考

### 提示词使用说明

新版业务需求、架构建议和第三方技术资料混合投放时使用。AI 必须把业务候选和技术候选分开。

```text
这是第 {{BATCH_NO}} 批资料。

资料类型：新版需求与技术参考
资料路径：{{PATH}}
资料版本：{{VERSION}}
优先级：{{PRIORITY}}
批内优先级：{{RULE 或 TBD}}

请：

1. 登记所有来源。
2. 分离业务需求、技术建议和 UI 建议。
3. 摘要新增、修改、保留和废弃内容。
4. 识别与既有资料的冲突。
5. 更新资料处理进度和项目看板。
6. 不把技术建议自动批准为架构。
7. 不把 UI 建议自动批准为 UI_SPEC。
8. 不生成最终 PRD。
```

## 8. 宣布材料投放完成

### 提示词使用说明

只有当前版本需要分析的资料已经全部投放时使用。该口令会冻结当前资料范围并开始正式计算文档进度。

```text
材料投放完成。

请将阶段切换为 BASELINE，并生成：

1. 来源盘点。
2. 决策基线。
3. 版本差异。
4. 冲突矩阵。
5. 待确认问题。
6. 需求一致性审计。

冻结当前资料范围。
初始化当前版本的文档计划和权重。
开始计算文档进度。

暂不生成 PRD、技术设计和正式 UI_SPEC。
```

# 第二部分：冲突处理和 PRD

## 9. 逐轮解决冲突

### 提示词使用说明

基线文档已经列出冲突和问题时使用。每轮只处理少量关键问题，避免一次确认过多规则。

```text
进入 CONFLICT_DECISION_REVIEW。

请从当前 P0 问题开始，按第 2A 节知情决策协议执行。
本轮只读，不修改决策基线、冲突矩阵、待确认问题或项目状态。

要求：

1. P0/P1 按高影响处理，每轮只解释 1 个冲突。
2. 低影响冲突每轮最多 2 项；同轮 2 项必须按各自 ID 分别提问、
   复述、二次确认和应用。
3. 先解释术语、来源约束和生活化例子。
4. 对每个方案完成第 2A 节规定的完整影响评估。
5. 每个决策最后只提出一个选择问题。
6. 我的初次选择不形成 DEC。
7. 我表示不理解或取消时，继续解释或保持待确认。
```

初次选择后使用：

```text
进入 CONFLICT_DECISION_CONFIRMATION。

本次只读。请复述：
- 冲突 ID 和主题
- 我的选择与限制条件
- 被拒方案
- 业务、数据、权限、状态、迁移、测试、发布和回滚影响
- 仍为 TBD 的内容

等待我明确回复“确认该决策”。
```

完成二次确认后，另行授权：

```text
进入 CONFLICT_DECISION_APPLY。

本次只应用以下已确认决策：
{{DECISION_ID、选择、限制条件}}

请形成 DEC，并同步决策基线、冲突矩阵、待确认问题、
来源登记和项目看板。不得应用未列出的候选。
```

## 10. PRD 准入审计

### 提示词使用说明

关键冲突处理完成、准备生成 PRD Draft 前使用。P0/P1 不为零时不得进入 PRD 生成。

```text
请执行 PRD 准入审计。

重点检查：

1. 金额和计算边界。
2. 支付、退款和结算。
3. 角色、权限和数据范围。
4. 状态流转和生命周期。
5. 账号和数据归属。
6. 正常、异常和重复操作结果。
7. Given/When/Then 是否可写。
8. 跨模块规则是否一致。
9. 技术或 UI 实现内容是否错误混入业务需求。

只有 P0/P1 为 0 才允许生成 PRD Draft。
审计发现需要用户裁决的 P0/P1 时，回到第 9 节并按第 2A 节执行；
不得在审计步骤直接生成 DEC。
技术问题继续标记为 PRD 后技术评估。
```

## 11. 生成 PRD Draft

### 提示词使用说明

PRD 准入审计通过后使用。该提示词只生成产品需求，不生成架构、数据库或具体 UI 规范。

```text
PRD 准入审计已通过。

请切换到 PRD_DRAFT，并生成：

1. PRODUCT_PRD。
2. 模块 PRD。
3. CHANGELOG。
4. TRACE_MATRIX。
5. OPEN_ITEMS。
6. DOCUMENT_PROGRESS。
7. PROJECT_DASHBOARD。

每条需求必须包含：

- 稳定编号
- 来源
- 新增/修改/保留/废弃状态
- 结论标签
- 角色和前置条件
- 主流程和异常流程
- 业务规则、状态和权限
- Given/When/Then

暂不生成技术设计和正式 UI_SPEC。
```

## 12. 评审候选需求

### 提示词使用说明

PRD Draft 中仍有候选和待评审需求时使用。初次选择只表示候选意向；
完成影响复述、二次确认和独立应用后才形成 DEC 并同步受影响文档。

```text
进入 CANDIDATE_REQUIREMENT_DECISION_REVIEW。

请列出 PRD 中所有候选和待评审需求，并按第 2A 节执行。
本轮只读，不修改 PRD 或进度状态。

1. 改变业务规则的候选按高影响处理，每轮只评审 1 条。
2. 低影响候选每轮最多 2 条。
   不得在同一轮混合高影响和低影响候选。
   同轮有 2 条低影响候选时，按各自 ID 分别复述、分别二次确认和应用。
3. 每条先解释当前规则、来源、术语、方案和完整影响。
4. 我的初次选择后只进入影响复述，不形成 DEC。
5. 我表示不理解或取消时，不更新任何文档。
```

初次选择后使用：

```text
进入 CANDIDATE_REQUIREMENT_DECISION_CONFIRMATION。

本次只读。请复述候选需求 ID、我的选择、限制条件、被拒方案、
业务和数据影响、测试验收影响以及剩余 TBD。
等待我明确回复“确认该决策”。
```

二次确认后另行授权：

```text
进入 CANDIDATE_REQUIREMENT_DECISION_APPLY。

本次只应用：
{{候选需求 ID、选择、限制条件}}

请形成 DEC，并更新 PRD、CHANGELOG、TRACE_MATRIX、OPEN_ITEMS、
决策基线、文档进度和项目看板。不得应用未列出的候选。
```

## 13. PRD 最终审计

### 提示词使用说明

候选需求评审完成、准备进入 In Review 前使用。该操作应以只读审计为主，先输出问题再决定是否修改。

```text
请对当前 PRD Draft 做最终审计。

检查：

1. 来源和编号。
2. 金额、权限、状态和异常。
3. 验收标准。
4. 模块依赖。
5. 开放项分类。
6. 追踪矩阵。
7. 文档状态和进度证据。
8. 是否存在未标记推断。
9. 是否混入技术或 UI 实现细节。

按 P0/P1/P2 输出发现。
P0/P1 不为 0 时不得进入 In Review。
审计中发现的新待确认项只登记为 QUESTION/CONFLICT；
完成第 2A 节二次确认和受控应用前不得直接写成 DEC。
```

## 14. PRD 进入 In Review

### 提示词使用说明

PRD 最终审计通过后使用。该口令不是批准 PRD，只是进入正式评审状态。

```text
PRD 最终审计已通过，P0/P1 为 0。

请将总 PRD、模块 PRD、开放项、追踪矩阵和变更记录
统一流转为 In Review。

保留 Draft 历史。
更新文档进度和项目看板。
不得标记 Approved 或 Baselined。
```

## 15. 批准 PRD

### 提示词使用说明

只有产品负责人或授权用户完成评审并明确批准时使用。PRD 批准属于高影响
状态变更；所有候选和冲突必须已按第 2A 节完成确认和应用。
AI 不得自行执行这一状态转换。
批准前先执行：

```text
进入 PRD_APPROVAL_CONFIRMATION。

本次只读。请复述 PRD 版本、批准范围、候选和冲突清零情况、
业务与数据影响、仍为 TBD 的配置项，以及本次不包含的 Baselined 和代码授权。
等待我明确回复“确认并应用批准：PRD_APPROVAL={{VERSION}}”。
```

下列明确批准口令只在上述复述完成时，同时构成二次确认和应用授权。

```text
确认并应用批准：PRD_APPROVAL={{VERSION}}。

请将总 PRD、模块 PRD、追踪矩阵和变更记录
更新为 Approved。

保留历史版本和状态。
更新文档进度和项目看板。
暂不标记 Baselined。
暂不修改业务代码。
```

# 第三部分：UI 逻辑范围同步

## 16. 根据 Approved PRD 生成 UI 逻辑范围

### 提示词使用说明

PRD Approved 后使用。这一步只确认哪些 PRD 模块需要哪些逻辑页面终端，不创建 `admin`、`miniapp` 等假设应用目录。

```text
PRD {{VERSION}} 已 Approved。

请执行 G5.5，根据 PRD 功能地图、模块 ID、目标终端和页面范围，
生成或同步 {{DOC_ROOT}}/ui-design/UI_SCOPE_MAP.md。

要求：

1. 登记 PRD 模块、逻辑页面终端和可选页面范围。
2. 使用稳定 PRD 模块 ID。
3. application_id、application_slug 和 repository_path 保持 TBD。
4. 创建 UI_APPLICATION_MAPPING.yaml 模板。
5. 更新 UI_DESIGN_REGISTER.yaml 和 UI_TRACE_MATRIX.md。
6. 不创建固定 admin、miniapp、app 或 h5 子目录。
7. 不虚构颜色、布局、组件和交互。
8. 废弃模块保留 Deprecated 和替代关系。
9. 应用架构确认前的 UI 素材先投放到 sources/inbox/，
   或登记为 application_id=TBD。
```

## 17. PRD 新增或调整模块后同步 UI 范围

### 提示词使用说明

Approved/Baselined PRD 通过 CR 新增、拆分、改名或废弃模块时使用。先更新逻辑范围；正式应用目录仅在已有应用映射时同步。

```text
PRD 已通过 {{CR_ID}} 新增或修改模块。

请比较新旧 PRD 并执行：

1. 更新 UI_SCOPE_MAP。
2. 更新 UI_DESIGN_REGISTER 和 UI_TRACE_MATRIX。
3. 改名但 ID 不变的模块只更新显示名称。
4. 拆分模块登记逻辑范围和替代关系。
5. 废弃模块标记 Deprecated。
6. 如果 UI_APPLICATION_MAPPING 已 Approved，同步实际应用下的功能目录。
7. 如果应用映射仍为 TBD，不创建假设应用目录。
8. 对已存在 UI_SPEC 的模块执行影响分析。
9. 输出待投放 UI 素材清单。

影响分析发现新的 UI 取舍或冲突时，复用第 24 节 UI 决策三步骤；
初次选择不得直接更新 UI_SPEC 或正式应用目录。
不要虚构新增模块的设计方案。
```

# 第四部分：总体架构和技术设计

## 18. 生成总体技术架构

### 提示词使用说明

PRD Approved 后、准备设计系统整体技术方案时使用。每项架构能力必须引用已批准需求。

第 18 节不再把“深度参与”和“直接生成”视为可被单条口令随意切换的两种写法，而是由受控模式决定：

1. `MANDATORY`：默认且推荐。先评审、确认和应用必选决策，再生成 Draft。
2. `OPTIONAL`：每次生成前由 AI 询问用户选哪条路径，AI 不得代选。
3. `DIRECT_DRAFT`：用户明确接受关键项保持 `TBD`，只生成待后续补决策的 Draft。

Cursor 的 Plan/Agent 模式只影响 AI 在 IDE 中如何工作，不等于本文的架构参与模式。拒绝 Cursor Plan Mode 不会自动切换为 `DIRECT_DRAFT`。

### 架构参与控制变量

推荐安全配置：

```text
ARCHITECTURE_PARTICIPATION_MODE=MANDATORY
ARCHITECTURE_DECISION_REVIEW_STATUS=NOT_STARTED
ARCHITECTURE_DECISION_APPLY_STATUS=NOT_STARTED
ALLOW_ARCHITECTURE_DRAFT_WITH_TBD=false
```

状态解释：

- `REVIEW_STATUS=NOT_STARTED`：还没有形成必选决策清单。
- `REVIEW_STATUS=IN_PROGRESS`：正在逐项询问，仍有必选项未确认。
- `REVIEW_STATUS=COMPLETED`：必选项全部完成影响复述和用户二次确认。
- `APPLY_STATUS=NOT_STARTED/PARTIAL`：确认结果尚未或只部分写入 ADR/受控记录。
- `APPLY_STATUS=COMPLETED`：全部必选确认结果已经写入。
- 任一状态为 `STALE`：上游发生变化，必须先重新评审或同步。

仅仅生成 ADR 候选清单、回答了部分问题或生成了架构 Draft，都不能自动把状态改为 `COMPLETED`。

### 步骤一：选择或确认参与模式

不确定当前模式时先发送：

```text
进入 ARCHITECTURE_PARTICIPATION_SETUP。

本次只核对总体架构参与模式，不生成或修改架构文档。

请读取当前项目变量并输出：

1. ARCHITECTURE_PARTICIPATION_MODE。
2. ARCHITECTURE_DECISION_REVIEW_STATUS。
3. ARCHITECTURE_DECISION_APPLY_STATUS。
4. ALLOW_ARCHITECTURE_DRAFT_WITH_TBD。
5. 当前仍未确认的高影响架构决策。

如果模式未设置，按第 2A 节把模式选择作为高影响决策：
先详细解释三种模式的门禁、风险、恢复成本和适用条件。
推荐 MANDATORY，但不要自动代填。
请让我选择：
- MANDATORY
- OPTIONAL
- DIRECT_DRAFT

我的初次选择后先复述影响；等待我二次确认后停止。
```

选择 `MANDATORY` 时，可以单独授权把变量固化到项目指南；只在对话中口头选择不等于已经修改项目变量。

### 步骤二：只读深度参与决策评审

```text
进入 ARCHITECTURE_DECISION_REVIEW。

ARCHITECTURE_PARTICIPATION_MODE=MANDATORY

本轮只做总体架构决策准备，不生成或修改正式技术文档，
不创建正式 ADR，不修改项目变量和进度状态。

请基于：
- Approved PRD {{VERSION}}
- UI_SCOPE_MAP {{PATH 或 NONE}}
- PRD 后技术评估问题
- 当前 TECH_STACK={{TECH_STACK 或 TBD}}
- 当前 REPOSITORY_ROLE={{REPOSITORY_ROLE}}

识别生成总体架构前必须确认的技术决策。

要求：

1. 按以下主题整理：
   - 技术栈
   - 应用与服务拆分
   - 仓库和项目结构
   - 多租户及数据隔离
   - 认证、权限和安全
   - 数据库、缓存、搜索和文件存储
   - 支付、退款、分账、幂等和一致性
   - 消息、任务和通知
   - 部署、环境、监控、容灾和回滚
2. 先区分：
   - G6 Draft 前必须确认
   - 可以延期到 G7～G9
3. 架构决策按高影响处理，每轮只提出 1 个。
4. 每个决策说明：
   - 这个决策在决定什么
   - PRD 和现有治理约束
   - 可选方案
   - 生活化例子
   - 各方案优缺点
   - 推荐方案和理由
   - 数据归属、依赖、事务和一致性影响
   - 对成本、复杂度、性能、交付和迁移的影响
   - 对安全、隐私、测试、发布和回滚的影响
   - 需要我确认的一个明确问题
5. 不替我选择存在明显权衡的方案。
6. 我初次回答后停止，不立即修改文件；下一步进入
   ARCHITECTURE_DECISION_CONFIRMATION 复述影响。
7. 用户确认前，未确定内容保持 TBD。
8. 不生成或修改 SYSTEM_CONTEXT、OVERALL_ARCHITECTURE、
   SECURITY_ARCHITECTURE、DEPLOYMENT_ARCHITECTURE、
   NON_FUNCTIONAL_REQUIREMENTS 或 ADR。

等待我的初次选择，不要把初次选择标记为已确认。
```

如果用户希望完全逐步参与，可补充：

```text
每次只问一个决策。
不要同时展示下一项。
我确认当前项后，再进入下一项。
```

### 步骤三：确认决策汇总

每轮回答后，要求 AI 只做确认，不写文件：

```text
进入 ARCHITECTURE_DECISION_CONFIRMATION。

本次不修改文件。

请把我刚才的回答整理为：

1. 决策主题。
2. 我选择的方案。
3. 我补充的限制条件。
4. 被拒绝的方案。
5. 对后续架构的影响。
6. 仍未确认的问题。

不得扩大我的选择范围。
如果我的回答存在歧义，只提一个澄清问题。
等待我回复“确认该决策”后停止。
```

“我倾向于”“可以考虑”、表示不理解、要求继续解释或取消问卷，
均不等于正式确认。建议使用：

```text
确认该决策：
{{DECISION_ID}}={{SELECTED_OPTION}}
限制条件：{{CONSTRAINTS 或 NONE}}
```

### 步骤四：应用已确认决策

评审和二次确认完成后，使用独立写入口令。它只应用决策，不同时生成全部架构：

```text
进入 ARCHITECTURE_DECISION_APPLY。

允许修改当前项目的架构决策登记、正式 ADR、
AI_DOCUMENTATION_GUIDE 和进度真源。

本次应用以下已有 `ARCHITECTURE_DECISION_CONFIRMATION` 证据的决策：

{{逐项列出 DECISION_ID、选择、限制条件}}

要求：

1. 为已确认决策创建或更新唯一 ADR。
2. ADR 记录背景、候选方案、最终选择、理由、影响、
   兼容、迁移、灰度和回滚。
3. 引用 Approved PRD 和相关技术评估项。
4. 不修改 PRD 业务规则。
5. 不应用未列出的候选决策。
6. 部分应用时：
   - ARCHITECTURE_DECISION_REVIEW_STATUS=IN_PROGRESS
   - ARCHITECTURE_DECISION_APPLY_STATUS=PARTIAL
7. 全部 G6 必选决策已经确认并应用时，才更新为：
   - ARCHITECTURE_DECISION_REVIEW_STATUS=COMPLETED
   - ARCHITECTURE_DECISION_APPLY_STATUS=COMPLETED
8. 本次不生成 MODULE_BOUNDARIES、模块详细技术设计、
   API、数据模型、UI 应用目录或开发批次。

完成后输出已应用决策、仍未决事项和状态。
不得自动提交 Git。
```

### 步骤五：门禁预检

生成架构前先单独检查：

```text
进入 ARCHITECTURE_GENERATION_PREFLIGHT。

本次只读检查，不修改文件。

请验证：

1. PRD 是否 Approved。
2. ARCHITECTURE_PARTICIPATION_MODE。
3. ARCHITECTURE_DECISION_REVIEW_STATUS。
4. ARCHITECTURE_DECISION_APPLY_STATUS。
5. ALLOW_ARCHITECTURE_DRAFT_WITH_TBD。
6. G6 必选决策是否全部有已确认和已应用证据。
7. 是否仍有会实质改变架构的关键 TBD。

输出 PASS 或 BLOCKED。

如果 BLOCKED：
- 列出阻断项。
- 给出下一条正确提示词。
- 不生成任何架构文档。
```

`MANDATORY` 模式只有在评审和应用状态均为 `COMPLETED` 时才能 PASS。

### 步骤六：生成总体架构 Draft

门禁 PASS 后使用：

```text
PRD {{VERSION}} 已 Approved。

ARCHITECTURE_PARTICIPATION_MODE=MANDATORY
ARCHITECTURE_DECISION_REVIEW_STATUS=COMPLETED
ARCHITECTURE_DECISION_APPLY_STATUS=COMPLETED
ALLOW_ARCHITECTURE_DRAFT_WITH_TBD=false

请切换到 ARCHITECTURE。

执行前先验证上述状态与项目真源一致；
如不一致，输出 BLOCKED 并停止，不得根据本口令改写状态。

门禁通过后生成：

1. SYSTEM_CONTEXT。
2. OVERALL_ARCHITECTURE。
3. SECURITY_ARCHITECTURE。
4. DEPLOYMENT_ARCHITECTURE。
5. NON_FUNCTIONAL_REQUIREMENTS。
6. ADR 候选清单。

要求：

1. 已确认技术决策引用对应正式 ADR。
2. 每项架构能力引用 PRD。
3. 区分当前实现、目标实现和差距。
4. 包含迁移、兼容、灰度和回滚。
5. 可延期到 G7～G9 的非关键项可以保持 TBD，并注明延期阶段。
6. 所有新产物状态为 Draft。
7. 不自动标记 Approved 或 Baselined。
8. 更新文档进度和项目看板。
9. 不生成 MODULE_BOUNDARIES、详细技术设计、API、数据模型、
   UI 应用目录或开发批次。
```

### 显式直接生成带 TBD 的 Draft

只有按第 2A 节充分解释风险并完成二次确认，明确接受跳过本轮决策时使用：

```text
我明确选择直接生成带未决项的总体架构 Draft。

ARCHITECTURE_PARTICIPATION_MODE=DIRECT_DRAFT
ALLOW_ARCHITECTURE_DRAFT_WITH_TBD=true

本次允许在关键技术决策尚未完成时生成 Draft，但要求：

1. 不得把推荐方案写成已确认决策。
2. ARCHITECTURE_DECISION_REVIEW_STATUS 不得自动改为 COMPLETED。
3. ARCHITECTURE_DECISION_APPLY_STATUS 不得自动改为 COMPLETED。
4. 所有未确认项保持 TBD。
5. 文档元信息记录 generation_mode=DIRECT_DRAFT_WITH_TBD。
6. 单独列出未决决策、风险和回到深度参与流程的步骤。
7. 状态只能是 Draft，不得 Approved/Baselined。
8. 本次授权使用后，将 ALLOW_ARCHITECTURE_DRAFT_WITH_TBD 恢复为 false，
   除非我明确要求保留。

然后按第 18 节生成六类架构产物。
```

### 已提前生成 Draft 的恢复流程

如果尚未完成决策却已经生成架构 Draft，不需要删除文件。使用：

```text
进入 ARCHITECTURE_DECISION_RECOVERY。

当前总体架构 Draft 在关键决策完成前已经生成。

本次只做恢复评估，不修改文件。

请：

1. 保留现有 Draft，不删除、不覆盖历史。
2. 对照 ADR 候选和架构文档列出：
   - 已确认并有证据的结论
   - 仅为推荐候选的结论
   - 仍为 TBD 的结论
   - 被误写成已确认的结论
3. 恢复建议状态：
   - ARCHITECTURE_DECISION_REVIEW_STATUS=IN_PROGRESS
   - ARCHITECTURE_DECISION_APPLY_STATUS=NOT_STARTED 或 PARTIAL
4. 给出后续逐项评审顺序。
5. 不生成新架构、不创建正式 ADR。

等待我重新进入 ARCHITECTURE_DECISION_REVIEW。
```

完成补充决策并应用后，再单独执行架构一致性同步；不要重新生成并覆盖整套 Draft。

## 19. 生成技术模块划分

### 提示词使用说明

总体架构草案形成后使用。它解决“模块详细技术设计中的模块名称从哪里来”的问题。

本节默认继承第 18 节参与模式。推荐配置：

```text
MODULE_BOUNDARY_PARTICIPATION_MODE=INHERIT
MODULE_BOUNDARY_DECISION_REVIEW_STATUS=NOT_STARTED
MODULE_BOUNDARY_DECISION_APPLY_STATUS=NOT_STARTED
ALLOW_MODULE_BOUNDARIES_DRAFT_WITH_TBD=false
```

需要覆盖继承模式时，先执行独立设置流程：

```text
进入 MODULE_BOUNDARY_PARTICIPATION_SETUP。

本次只读，不修改项目变量、MODULE_BOUNDARIES 或进度状态。

请读取并解释：

1. 当前 ARCHITECTURE_PARTICIPATION_MODE。
2. 当前 MODULE_BOUNDARY_PARTICIPATION_MODE 和继承后的有效值。
3. INHERIT、MANDATORY、OPTIONAL、DIRECT_DRAFT 的门禁差异。
4. 对未决争议、返工、恢复路径和进入 G8 的影响。

模式覆盖属于高影响决策，按第 2A 节只提出一个选择问题。
我的初次选择后停止，不修改变量。
```

```text
进入 MODULE_BOUNDARY_PARTICIPATION_CONFIRMATION。

本次只读。请复述原模式、选择的新模式、限制条件、
门禁与风险影响，以及 DIRECT_DRAFT 是否同时需要一次性授权。
等待我明确回复“确认该决策”。
```

```text
进入 MODULE_BOUNDARY_PARTICIPATION_APPLY。

本次只应用：
MODULE_BOUNDARY_PARTICIPATION_MODE={{已确认值}}
限制条件：{{CONSTRAINTS 或 NONE}}

只更新参与模式和受影响的门禁说明，不生成 MODULE_BOUNDARIES，
不自动修改 REVIEW_STATUS/APPLY_STATUS。
```

当继承结果为 `MANDATORY` 时，必须先评审和应用模块拆分争议，不能直接发送生成口令绕过。

### 按参与模式执行模块边界评审

```text
进入 MODULE_BOUNDARY_DECISION_REVIEW。

MODULE_BOUNDARY_PARTICIPATION_MODE={{INHERIT/MANDATORY/OPTIONAL/DIRECT_DRAFT}}

本轮只做技术模块划分决策准备，不生成或修改正式 MODULE_BOUNDARIES
或模块详细技术设计，不修改项目变量和进度状态。

请基于：
- Approved PRD {{VERSION}}
- 总体架构 Draft
- 已确认的架构决策
- PRD 业务模块和跨模块依赖

识别 MODULE_BOUNDARIES 形成前需要用户确认的拆分争议。

要求：

1. 模块边界按高影响处理，每轮只提出 1 个拆分争议。
2. 每项说明：
   - 这个边界实际在决定什么
   - 相关 PRD 与总体架构约束
   - 专业术语和生活化运行例子
   - 当前模块边界候选
   - 可选拆分或合并方案
   - 各方案优缺点
   - 推荐方案和理由
   - 对数据所有权、依赖方向和事务一致性的影响
   - 对成本、复杂度、性能和安全隐私的影响
   - 对独立测试、部署、发布、迁移和回滚的影响
   - 需要用户确认的问题
3. 不按页面数量或数据库表机械拆分。
4. 不提前按微服务过度拆分。
5. 用户确认前，技术模块名称、稳定 ID、拆分关系和部署关系保持候选或 TBD。
6. 我初次选择后停止，不立即修改文件；下一步进入
   MODULE_BOUNDARY_DECISION_CONFIRMATION 复述选择和影响。
7. 不生成全部模块详细技术设计。

我表示不理解、要求继续解释或取消时，停留在当前争议。
```

### 确认模块边界决策

每个拆分争议初次选择后使用：

```text
进入 MODULE_BOUNDARY_DECISION_CONFIRMATION。

本次只读，不修改文件和状态。

请复述：

1. 争议 ID 和边界主题。
2. 我选择的方案和限制条件。
3. 被拒绝的方案。
4. 职责和非职责。
5. 数据所有权、依赖方向和事务边界。
6. 对成本、性能、测试、部署、发布、迁移和回滚的影响。
7. 继续保持 TBD 或延期到 G8/G9 的事项。

不得扩大我的选择。存在歧义时只问一个澄清问题。
等待我明确回复“确认该决策”。
```

取消问卷、表示不理解、要求继续解释或仅表达倾向时，不记录决定，
不更新评审状态，返回 `MODULE_BOUNDARY_DECISION_REVIEW`。

### 应用模块边界决策

完成影响复述和二次确认后，先独立应用：

```text
进入 MODULE_BOUNDARY_DECISION_APPLY。

本次只应用以下已有 `MODULE_BOUNDARY_DECISION_CONFIRMATION` 证据的模块拆分决策：

{{逐项列出争议 ID、选择、限制条件}}

要求：

1. 写入受控决策记录并引用 PRD、总体架构和相关 ADR。
2. 不生成模块详细技术设计。
3. 不应用未列出的候选。
4. 部分完成时：
   - MODULE_BOUNDARY_DECISION_REVIEW_STATUS=IN_PROGRESS
   - MODULE_BOUNDARY_DECISION_APPLY_STATUS=PARTIAL
5. 全部必选争议确认并应用后才能设为：
   - MODULE_BOUNDARY_DECISION_REVIEW_STATUS=COMPLETED
   - MODULE_BOUNDARY_DECISION_APPLY_STATUS=COMPLETED
6. 不自动提交 Git。
```

### 生成 MODULE_BOUNDARIES Draft

```text
总体架构草案已经形成。

MODULE_BOUNDARY_PARTICIPATION_MODE={{当前有效值}}
MODULE_BOUNDARY_DECISION_REVIEW_STATUS={{当前状态}}
MODULE_BOUNDARY_DECISION_APPLY_STATUS={{当前状态}}
ALLOW_MODULE_BOUNDARIES_DRAFT_WITH_TBD={{true/false}}

请先执行门禁预检：

- MANDATORY：评审和应用状态必须均为 COMPLETED。
- OPTIONAL：未完成时按第 2A 节解释评审与直接 Draft 的差异和风险，
  完成二次确认前停止本次生成。
- DIRECT_DRAFT：必须同时明确 ALLOW_MODULE_BOUNDARIES_DRAFT_WITH_TBD=true。

门禁不满足时输出 BLOCKED，不得根据本口令改写状态。

门禁通过后：

请生成 MODULE_BOUNDARIES：

1. PRD 业务模块。
2. 技术模块和稳定 ID。
3. 职责和非职责。
4. PRD 到技术模块映射。
5. 数据所有权。
6. 依赖方向。
7. 事务和一致性边界。
8. 测试、发布和回滚边界。
9. 技术设计文档清单。
10. 拆分争议和候选方案。

所有新产物先标记 Draft。
未确认项不得写成已批准结论。
先评审模块划分，不立即生成全部详细设计或进入 G8。
不要按页面数量或数据库表机械拆分。
```

如明确需要带争议直接生成，必须先按第 2A 节解释风险并完成二次确认，
再显式设置：

```text
MODULE_BOUNDARY_PARTICIPATION_MODE=DIRECT_DRAFT
ALLOW_MODULE_BOUNDARIES_DRAFT_WITH_TBD=true
```

并要求记录未决争议、风险和恢复步骤；该授权不能自动继承到后续阶段。

## 20. 生成单模块详细技术设计

### 提示词使用说明

MODULE_BOUNDARIES 已经批准后，针对一个技术模块使用。一次只生成一个模块，避免跨模块失控。

```text
MODULE_BOUNDARIES 已批准。

请为以下技术模块生成详细技术设计：

模块 ID：{{TECH_MODULE_ID}}
模块名称：{{TECH_MODULE_NAME}}

要求：

1. 只设计该模块。
2. 引用 PRD、总体架构和模块边界。
3. 包含组件、调用流程和状态机。
4. 包含 API、数据、事务、幂等和并发。
5. 包含权限、安全、审计、异常、重试和补偿。
6. 包含性能、迁移、灰度、回滚和测试。
7. 发现 PRD 缺口时创建 CR，不自行改变业务规则。
```

## 21. 生成 API、数据和编码规范

### 提示词使用说明

相关模块技术设计已评审，技术栈也已确定后使用。API、数据和统一规范必须各自保持单点真源。

```text
相关模块技术设计已经评审，目标技术栈已经确认。

请生成：

1. API 契约和 OpenAPI 规划。
2. 数据模型。
3. 数据字典。
4. 数据迁移和回滚计划。
5. 项目结构规范。
6. 编码规范。
7. API、数据库、测试、Git 和 Code Review 规范。

api/ 是 API 唯一真源。
database/ 是数据唯一真源。
standards/ 是工程规范唯一真源。
模块设计只引用这些文档，不复制第二套定义。
```

## 21A. 根据技术架构生成正式 UI 应用目录

### 提示词使用说明

总体应用架构、模块边界和 `standards/PROJECT_STRUCTURE.md` 已确认后使用。正式 `products/` 子目录必须对应真实前端应用，而不是固定使用 `admin` 或 `miniapp`。

```text
总体应用架构、MODULE_BOUNDARIES 和 PROJECT_STRUCTURE.md 已确认。

请执行 G9.1，根据以下文档生成正式 UI 应用目录：

- architecture/OVERALL_ARCHITECTURE.md
- architecture/MODULE_BOUNDARIES.md
- standards/PROJECT_STRUCTURE.md
- ui-design/UI_SCOPE_MAP.md

要求：

1. 识别真实前端应用交付单元。
2. 为每个应用登记 application_id、application_slug、
   repository_path、application_type 和 devices。
3. 更新 UI_APPLICATION_MAPPING.yaml。
4. 按 ui-design/products/{application_slug}/ 生成目录。
5. application_slug 默认与技术架构中的项目目录名一致。
6. 例如 app/admin_front 映射为 products/admin_front。
7. 只生成实际存在的应用。
8. 项目只有 admin_front 时，不生成 miniapp、app 或 h5。
9. 后端服务没有页面交付单元时不生成 UI 应用目录。
10. 将 PRD 模块映射到实际应用、设备和功能目录。
11. 创建 README、UI_SPEC 和 materials 模板。
12. 更新 UI_DESIGN_REGISTER 和 UI_TRACE_MATRIX。

只生成目录、映射和模板，不虚构视觉方案。
```

## 22. 技术文档终审和基线

### 提示词使用说明

总体架构、模块设计、API、数据和编码规范已经完成时使用。Baselined
属于高影响状态变更，只有先完成第 2A 节影响复述，并由用户在审计通过后
再次明确批准，才能执行。

```text
请审计以下文档的一致性：

- PRD
- 总体架构
- MODULE_BOUNDARIES
- 模块技术设计
- API 契约
- 数据模型和迁移
- 编码规范

重点检查需求映射、状态、权限、数据、事务、幂等、迁移、兼容和回滚。

先输出 P0/P1/P2，并复述基线范围、不可变约束、迁移和回滚影响。
P0/P1 为 0 后停止，等待我使用
“确认该决策：TECH_BASELINE={{VERSION}}”再次明确批准；
收到该口令后再将当前交付版本 Baselined。
同步追踪矩阵、文档进度和项目看板。
```

# 第五部分：可选 UI 设计规范

## 23. 明确跳过 UI 规范

### 提示词使用说明

当前项目没有设计材料，或本次迭代不需要额外 UI 约束时使用。跳过 UI 阶段不得阻断开发。

```text
本项目本次不启用正式 UI 规范生成。

ENABLE_UI_SPEC_GENERATION=false
UI_SPEC_ENABLED_APPLICATIONS=
UI_CONSTRAINT_MODE=NONE

UI_SCOPE_MAP 和实际应用目录映射保留，用于未来模块追踪。

请直接根据 PRD、技术设计、编码规范、现有组件和现有 UI 基线
继续生成开发批次。

UI_SPEC 缺失不得作为阻塞项，也不计入文档进度分母。
未点名时不得启动视觉修复循环。
```

## 23A. 按应用启用正式 UI 规范

### 提示词使用说明

只想给某一个前端应用走 G9.5，其它应用继续关时使用。打开分闸会改变验收基准，必须走第 2A 节。slug 必须来自 `UI_APPLICATION_MAPPING.yaml`，禁止虚构。

```text
进入 UI_SPEC_SCOPE_DECISION_REVIEW。

本次只读。请按第 2A 节解释：是否只启用下列前端应用的正式 UI 规范。

拟议总闸：ENABLE_UI_SPEC_GENERATION=true
拟议分闸：UI_SPEC_ENABLED_APPLICATIONS={{APPLICATION_SLUG，例如 gongyi_online}}
其它应用：保持关闭，占位 UI_SPEC 不能当验收。

请说明：对功能开发是否仍不阻断、对有设计图页的还原等级、
与未批准 anydesign 产物的关系、回滚方式（把该 slug 移出名单）。
初次选择后停止，不改项目变量。
```

二次确认并应用后再写入 `PROJECT_VARIABLES.md`。未完成 `DECISION_APPLY` 不得生成该应用正式 UI_SPEC。

## 24. 只使用组件库生成 UI 基线

### 提示词使用说明

没有具体设计图，但项目明确使用 Ant Design、Element Plus、TDesign、Vant 等组件库时使用。

```text
请启用可选 UI 设计规范：

ENABLE_UI_SPEC_GENERATION=true
UI_SPEC_ENABLED_APPLICATIONS={{APPLICATION_SLUG}}
UI_CONSTRAINT_MODE=DESIGN_SYSTEM_ONLY

前端应用：{{APPLICATION_SLUG}}
设备：{{DEVICE}}
组件库：{{LIBRARY}}
组件库版本：{{VERSION}}
其他规范资料：{{PATH 或 NONE}}

请先扫描资料，然后按第 2A 节通过知情决策问答生成：

1. 前端应用级 UI_SPEC。
2. 设备 UI_SPEC。
3. 页面布局模式。
4. 组件映射和使用边界。
5. 表格、表单、弹窗和反馈规范。
6. Loading、Empty、Error 和 No Permission。
7. 响应式和可访问性。
8. 自定义边界和禁止事项。

改变业务字段、权限、流程、隐私或正式验收基准的问题按高影响处理，
每轮只处理 1 项；其它低影响 UI 决策每轮最多 2 项。
初次选择后先复述影响并二次确认；完成 `UI_SPEC_DECISION_APPLY` 后才生成 Draft。
未经批准不得标记 Approved。
```

第 24、28、30、31、32 节中的 UI 决策统一使用以下三个独立步骤：

```text
进入 UI_SPEC_DECISION_REVIEW。

本次只读。按第 2A 节解释当前 UI 决策、素材和 PRD 约束、
2～3 个方案及完整影响。初次选择后停止，不修改 UI_SPEC、
UI-DEC、CR、素材清单或项目状态。
```

```text
进入 UI_SPEC_DECISION_CONFIRMATION。

本次只读。复述 UI 决策 ID、选择、限制条件、被拒方案、
视觉与业务影响、F3/F4 资格、测试发布回滚影响和剩余 TBD。
等待我明确回复“确认该决策”。
```

```text
进入 UI_SPEC_DECISION_APPLY。

本次只应用：
{{UI 决策 ID、选择、限制条件}}

按类型写入 UI-DEC；影响业务时创建 CR。然后只更新已确认范围内的
UI_SPEC、素材登记、追踪和状态，不应用未列出的候选。
```

## 25. 投放某个 UI 节点的设计素材

### 提示词使用说明

向前端应用、设备、功能模块或页面目录添加设计规范、设计图、品牌资料或组件说明后使用。

```text
以下 UI 设计节点增加了原始素材：

节点：{{UI_NODE_PATH}}
素材路径：{{MATERIALS_PATH}}
适用版本：{{TARGET_VERSION}}
目标终端：{{TARGET}}
期望还原等级：{{F0/F1/F2/F3/F4}}

请：

1. 扫描材料目录。
2. 为素材分配 SRC-UI 或 SRC-STD 编号。
3. 更新 MATERIALS_MANIFEST.yaml。
4. 记录路径、类型、版本、哈希、状态、范围和版权。
5. 区分明确事实、推断和待确认项。
6. 对照 PRD 检查冲突。
7. 输出素材完整性和缺失项。

当前先登记素材，不直接批准 UI_SPEC。
```

## 25A. 使用 anydesign 解析设计图

### 提示词使用说明

已有 PNG/JPG 或 URL/Figma，需要抽出统一令牌和组件清单时使用。本步只产素材，不批准 UI_SPEC。未安装时按主提示词十八之二开机检查。输出必须落到该应用 `products/{slug}/materials/`，禁止复制进每个 `devices/`，禁止写入 `shared/`（除非确认是跨产品品牌）。

**收工三行（必写）：** 当前停在第 25A 节。下一刀只能是令牌确认（若有 Open Questions）或第 28A 节 Draft UI_SPEC。禁止把这一步说成换皮、改业务代码或 UI_SPEC Approved。令牌已确认仍不等于可换皮。

```text
请使用 anydesign 分析以下设计图：

应用：{{APPLICATION_SLUG，例如 gongyi_online}}
路径：{{DESIGN_IMAGE_DIR 或 单文件路径}}
文件：{{文件名列表或 ALL}}

请：
1. 若 Skill 或 Python 依赖缺失，按 ANYDESIGN_TOOLS_PATH 开机安装后执行；失败则停止。
2. 提取统一令牌（颜色、字体、间距、圆角、阴影），输出 W3C DTCG 到
   {{DOC_ROOT}}/ui-design/products/{{APPLICATION_SLUG}}/materials/tokens/design-tokens.json
3. 重复组件清单、重建说明、不确定项写入
   {{DOC_ROOT}}/ui-design/products/{{APPLICATION_SLUG}}/materials/design-docs/design.md
4. 更新该节点 MATERIALS_MANIFEST.yaml 与 UI_DESIGN_REGISTER.yaml
5. 覆盖范围写 Partial（若未覆盖全部页面）
6. 不得标记 UI_SPEC Approved，不得改业务代码
7. 收工必须报三行：当前第 25A 节；下一刀令牌确认或第 28A 节；禁止说成换皮
```

## 26. 提交自定义图标资产

### 提示词使用说明

设计图包含自定义图标，且已经取得 SVG、PNG、WebP、iconfont 或动画源文件时使用。高保真页面应优先使用此方式。

```text
以下 UI 节点新增自定义图标资产：

节点：{{UI_NODE_PATH}}
图标目录：{{ICON_ASSET_PATH}}
目标平台：{{WEB/MINIAPP/APP/MULTI}}
来源：{{FIGMA/SKETCH/PSD/PROJECT/OTHER}}
版权或许可：{{LICENSE}}

请：

1. 扫描 SVG、PNG、WebP、ICO、iconfont、Lottie JSON 和 GIF。
2. 原始母版放入 source/。
3. 各平台导出文件放入 exports/{target}/。
4. 更新 ICON_MANIFEST.yaml。
5. 登记 asset_id、名称、格式、版本、目标平台、状态、许可和哈希。
6. 检查重复资产和替代关系。
7. 检查 SVG 和目标小程序运行时的兼容性。
8. 未经第 2A 节知情决策二次确认，不得改变图标形状、颜色或语义。
```

## 27. 从设计截图提取或重建图标

### 提示词使用说明

只有 PNG/JPG/WebP 页面设计截图，没有原始图标文件时使用。截图提取只能作为降级方案，不能宣称恢复了原始 SVG。

```text
进入 ICON_EXTRACTION_DECISION_REVIEW。

以下设计截图中存在需要提取的图标：

UI 节点：{{UI_NODE_PATH}}
设计图：{{IMAGE_PATH}}
目标平台：{{TARGET}}
允许的处理方式：
- {{裁剪位图：是/否}}
- {{匹配标准组件库：是/否}}
- {{AI 近似重建 SVG：是/否}}

本轮只读，不生成资产，不更新 ICON_MANIFEST。

请：

1. 识别图标位置、尺寸、颜色和可见状态。
2. 按第 2A 节比较裁剪位图、标准组件库匹配和 AI 近似重建，
   说明清晰度、版权、语义、可维护性、平台兼容、F4 资格和回滚影响。
3. 即使上面的允许方式已预填，涉及 AI 重建、改变语义或 F4 基准时，
   仍须完成影响复述和二次确认。
4. 推荐一个方案并说明适用条件，最后只提出一个选择问题。
5. 初次选择后停止，不生成或修改任何资产。
```

初次选择后使用：

```text
进入 ICON_EXTRACTION_DECISION_CONFIRMATION。

本次只读。请复述图标资产 ID、选择的处理方式、限制条件、
版权与平台兼容影响、F4 资格、被拒方案和仍无法恢复的细节。
等待我明确回复“确认该决策”。
```

完成二次确认后另行授权：

```text
进入 ICON_EXTRACTION_DECISION_APPLY。

本次只应用：
{{图标资产 ID、处理方式、限制条件}}

请：

1. 优先匹配已批准的组件库图标。
2. 无匹配时按已确认方案裁剪或近似重建。
3. 裁剪文件标记 source_type=cropped。
4. AI 重建文件放入 reconstructed/。
5. AI 重建图标标记 source_type=reconstructed、status=Needs Review。
6. 记录无法从截图恢复的路径、描边、透明区域和矢量细节。
7. 更新 ICON_MANIFEST.yaml。
8. 输出待人工确认图标清单。

未经人工确认，AI 重建图标不得用于 F4 视觉基准。
```

## 28. 根据原始素材首次生成 UI_SPEC

### 提示词使用说明

某个 UI 节点的素材已经登记完成时使用。AI 会先按第 24 节的
`UI_SPEC_DECISION_REVIEW/CONFIRMATION/APPLY` 提问，再生成该层级的正式规范草案。

```text
请为以下 UI 节点首次生成 UI_SPEC：

节点：{{UI_NODE_PATH}}
目标版本：{{TARGET_VERSION}}

请执行：

1. 检查 MATERIALS_MANIFEST.yaml 和 ICON_MANIFEST.yaml。
2. 若存在 anydesign 产物，读取 design.md 与 design-tokens.json，
   按主提示词第 6～9、17 节映射；不确定项不得写成已确认。
3. 读取设计规范、设计图、组件库、品牌和图标资料。
4. 读取父级 Approved/Baselined UI_SPEC。
5. 对照 PRD 检查冲突。
6. 按第 2A 节处理冲突和待确认项；高影响每轮 1 项，低影响最多 2 项。
7. 初次选择后先复述影响；二次确认和受控应用完成、信息足够后，
   才生成该节点 UI_SPEC.md。
8. 明确继承、覆盖、locked 和禁止事项。图不全时标记 Partial；没图页 F0。
9. 标记还原等级和视觉验收标准。
10. 未确认内容标记 TBD。
11. 先生成 Draft，未经批准不得标记 Approved。
```

## 28A. 仅为已分闸应用生成 Draft UI_SPEC

### 提示词使用说明

第 23A 节已应用、anydesign 或其它素材已登记后，给该应用出 Draft。其它应用不得顺带生成正式规范。

用户若已预定第一组页面，只写入覆盖范围 Partial，**不构成换皮开工**。

**收工三行（必写）：** 当前停在第 28A 节 Draft。下一刀只能是第 29 节批准。禁止把这一步说成换皮或改业务代码。

```text
请执行 G9.5，仅为以下应用生成 Draft UI_SPEC：

应用：{{APPLICATION_SLUG}}
节点：{{DOC_ROOT}}/ui-design/products/{{APPLICATION_SLUG}}/
输入：anydesign 产物（若有）、Approved PRD、父级 shared（若无则注明）。
覆盖：{{Partial 或 Full}}

总闸与分闸必须已经包含该 slug。未包含则停止，改走第 23A 节。
不得 Approved。不得改未列入的应用。不得改业务代码。
用户预定的页面名单只写入覆盖范围。
收工三行：当前第 28A 节；下一刀第 29 节；禁止说成换皮。
```

## 29. 批准 UI_SPEC

### 提示词使用说明

设计负责人或授权用户已经完成 UI_SPEC 评审时使用。所有高影响 UI 问答
必须已按第 2A 节完成二次确认和受控应用；只有批准后的规范才能约束正式开发。

批准前先执行：

```text
进入 UI_SPEC_APPROVAL_CONFIRMATION。

本次只读。请复述 UI_SPEC ID、版本、适用范围、继承与 locked 影响、
F3/F4 验收影响、剩余 TBD，以及本次不包含的代码修改授权。
等待我明确回复
“确认并应用批准：UI_SPEC_APPROVAL={{UI_SPEC_ID}}@{{VERSION}}”。
```

下列批准口令只在上述复述完成时，同时构成二次确认和应用授权。

```text
确认并应用批准：UI_SPEC_APPROVAL={{UI_SPEC_ID}}@{{VERSION}}

UI_SPEC：{{UI_SPEC_ID}}
版本：{{VERSION}}
适用范围：{{SCOPE}}

请：

1. 将状态更新为 Approved。
2. 保留 Draft 历史。
3. 更新 UI_DESIGN_REGISTER。
4. 更新 UI_TRACE_MATRIX。
5. 记录批准人和批准日期。
6. 标记继承该规范且需要复核的下级节点。
7. 更新文档进度和项目看板。
8. 收工三行：当前第 29 节已批准；下一刀才是点名页换皮（须另开口令）；本批准口令不改代码。

暂不自动修改代码。
```

## 30. 原始素材变化后增量更新 UI_SPEC

### 提示词使用说明

已有 UI_SPEC 后，设计目录增加、替换或废弃素材时使用。存在选择时复用
第 24 节三个 UI 决策步骤；该流程不会无条件重写所有 UI 文档。

```text
以下 UI 设计节点增加、修改或废弃了素材：

节点：{{UI_NODE_PATH}}
变更说明：{{CHANGE_SUMMARY}}

请：

1. 比较 MATERIALS_MANIFEST、ICON_MANIFEST 和文件哈希。
2. 识别新增、修改、废弃和未变化素材。
3. 分析对当前 UI_SPEC 的影响。
4. 按第 2A 节通过知情决策解决冲突和缺失信息；
   初次选择不得直接改写规范。
5. 完成影响复述、二次确认和 `UI_SPEC_DECISION_APPLY` 后，
   生成 UI_SPEC 新版本，不覆盖历史版本。
6. 将新版本先标记为 Draft。
7. 审计继承该节点的下级规范。
8. 将受影响下级规范标记 Needs Review。
9. 更新受影响的批次 UI Contract。
10. 未受影响文档保持不变。
```

## 31. 高保真页面规范

### 提示词使用说明

页面要求接近百分百还原，并且已经有批准的页面设计图和必要资产时使用。
F3/F4 选择复用第 24 节三个 UI 决策步骤；F4 必须固定测试环境和视觉基准。

```text
请为以下页面生成高保真 UI 规范：

UI 节点：{{PAGE_NODE_PATH}}
UI_CONSTRAINT_MODE=HIGH_FIDELITY
还原等级：{{F3/F4}}
设计图版本：{{DESIGN_VERSION}}
目标设备：{{DEVICE}}
目标浏览器或运行时：{{RUNTIME}}
目标视口：{{VIEWPORT}}
字体环境：{{FONT_ENV}}
组件库版本：{{LIBRARY_VERSION}}

请按第 2A 节逐项确认；F3/F4 视觉基准均按高影响处理：

1. 设计图、字体、Logo 和自定义图标是否齐全。
2. 固定测试数据和动态区域。
3. Loading、Empty、Error、No Permission 等状态。
4. 响应式和安全区域。
5. 允许差异和禁止差异。
6. 像素、颜色、间距和字体的验收方式。
7. 截图基线和视觉差异阈值。
8. 视觉回归测试方式。

存在缺失时按第 2A 节先解释缺口、备选处理及影响，不开始编码。
F3/F4 视觉基准属于高影响决策，每轮只确认 1 项；二次确认前不得开始实现。
只有截图而缺少原始图标资产时不得直接声明达到 F4。
```

## 32. 设计与 PRD 冲突

### 提示词使用说明

设计图、UI_SPEC 与 Approved PRD 的字段、流程、权限或状态不一致时使用。
裁决复用第 24 节三个 UI 决策步骤；业务规则始终由 PRD 决定。

```text
发现 UI 设计与 PRD 不一致。

冲突范围：{{SCOPE}}
设计来源：{{SRC_UI_ID}}
关联需求：{{REQ_ID}}
冲突说明：{{DESCRIPTION}}

请：

1. 停止冲突部分进入开发。
2. 创建 UI-CONFLICT。
3. 判断是纯视觉冲突还是业务冲突。
4. 纯视觉冲突通过 UI-DEC 处理。
5. 业务目标、字段、权限、状态或流程变化时创建 CR。
6. 按第 2A 节执行；涉及业务字段、权限、状态、流程或 F4 基准时，
   每轮只处理 1 个高影响问题。
7. 用户初次选择后先复述影响；二次确认并完成 `UI_SPEC_DECISION_APPLY` 后，
   才更新设计、PRD 或 UI_SPEC。
8. 只有 `UI_SPEC_DECISION_APPLY` 完成后，才更新 UI_TRACE_MATRIX、
   批次 UI Contract 和项目看板。
```

## 32A. 视觉修复循环（独立手续）

### 提示词使用说明

要对某一页和设计图做截图对照并只修差异时使用。静态预览写完、开发中或功能验收后均可。不是 G 阶段。必须加载 `.cursor/skills/visual-repair-loop/`。不要求已启用 UI_SPEC 或已跑 anydesign。禁止业务路由 `/v2`。designfit 不成门禁。

```text
进入视觉修复循环。本次不是 G 阶段。

应用：{{APPLICATION_SLUG，例如 gongyi_online}}
页面：{{路由或 ui-lab 路径}}
设计图：{{PNG 路径}}
时机：静态 | 验收
视口：{{默认 390x844}}
最大圈数：{{默认 4}}
允许修改：仅该页及其点名的公共零件

请加载 visual-repair-loop Skill，按主提示词二十二之二：
建档 → 先出 DIFF-1 再改 → 只修清单 → 复比直到通过或达最大圈数停止。
打不开页面则停止。不改未点名页。不借机改接口、路由或需求。
有已确认令牌则必须引用。没有令牌也能跑，但不得写「已按批准 UI_SPEC 验收」。
```

停手或通过后使用：

```text
视觉修复循环结束。请输出 RESULT.md：通过/未通过/用户叫停、剩余 DIFF、改动文件。
不要开始未点名的下一页。
```

# 第六部分：开发批次和代码实施

## 33. 生成批次有效 UI Contract

### 提示词使用说明

相关 UI_SPEC 已 Approved/Baselined，准备生成前端开发批次时使用。开发人员只需要以合并结果作为当前批次 UI 入口。

```text
请为以下开发批次生成有效 UI 开发约束：

批次：{{BATCH_ID}}
前端应用：{{APPLICATION_SLUG}}
设备：{{DEVICE}}
PRD 模块：{{PRD_MODULES}}
页面：{{PAGES}}

请按以下顺序加载并合并 Approved/Baselined UI_SPEC：

shared
→ 前端应用
→ 设备
→ 功能模块
→ 页面

请输出：

1. EFFECTIVE_UI_CONTRACT.md。
2. VISUAL_ACCEPTANCE.md（固定视口、设计图路径、差异阈值；
   建议调用第 32A 节循环，不得写成必须先做完 G11 的门禁；
   designfit 可选）。
3. REFERENCE_MANIFEST.yaml。
4. 每条规则的来源层级和规范版本。
5. 继承、覆盖和 locked 明细。
6. 设计图、图标和组件库引用。
7. 冲突和待确认项。

不得使用 Draft、Needs Review 或未确认 AI 重建资产约束正式开发。
冲突和待确认项只列入 OPEN_ITEMS，不得合并为有效规则；
先按第 2A 节确认并应用，再重新生成 Contract。
该前端应用必须已在 UI_SPEC_ENABLED_APPLICATIONS 中。未点名页面不得扩大进视觉验收。
```

## 34. 生成开发批次

### 提示词使用说明

PRD、架构和技术设计已 Baselined 后使用。如果启用了 UI 规范，先确保对应批次 UI Contract 已生成。

```text
当前版本的 PRD、架构和相关技术设计已经 Baselined。

请切换到 DELIVERY，按依赖生成开发批次。

每批必须：

1. 只围绕一个业务目标。
2. 关联 PRD 和技术设计。
3. 可独立开发、测试、验收和回滚。
4. 说明代码、数据、API 和配置影响。
5. 分配权重和负责人。
6. 进入 DEVELOPMENT_PROGRESS。
7. 更新项目看板。

前端批次额外记录：

- UI_CONSTRAINT_MODE
- EFFECTIVE_UI_CONTRACT（可空）
- 设计和图标来源（可空）
- 视觉验收（可空）

UI_CONSTRAINT_MODE=NONE 时不得因此阻断批次进入 Ready。
```

## 35. 开始单个开发批次

### 提示词使用说明

一个批次已经 Ready，且用户允许修改代码时使用。每次只实施一个批次。

自测约束（v3.1.13）：不得为了过验收改口径。临时桩、绕过、写死、假装成功，测完必须拆掉。测出产品偏离已确认口径，修掉并留下；要改规则先走第 2A 节 / CR，不得把演示改成新口径。灌库、改测试数据不算改口径。本节不管第 36A 节。

```text
允许修改业务代码：true
当前阶段：IMPLEMENTATION

本次只实施：

批次：{{BATCH_ID}}
名称：{{BATCH_NAME}}
负责人：{{DEVELOPER}}

开始前检查：

1. 关联 PRD。
2. 技术设计。
3. API 和数据契约。
4. 编码规范。
5. EFFECTIVE_UI_CONTRACT 和视觉验收（如启用）。
6. 图标和视觉资产状态。
7. Git 状态。
8. 测试计划。
9. 自测约束：不得为了过验收改口径；临时桩测完必须拆掉；测出产品偏离已确认口径则修掉并留下；要改规则先走第 2A 节。灌库和改测试数据不算改口径。

开始前还须确认上一批次已完成第 36A 节：有待收口口径则 CR 已收入 PRD，否则已记录“本批无待收口 PRD 变更”。未完成不得将本批次升为 In Progress。

创建 WORKLOG。
将批次更新为 In Progress。
更新开发进度。
不要修改其他批次或重构无关模块。
本批次结束、进入下一批前走第 36A 节。
若已启用白话（存在 {{DOC_ROOT}}/progress/PLAIN_SYNC.yaml），第 36A 节之后走第 36D 节。
临时袋未纳入前，不得把袋内口径当成已基线 PRD 来开工。
```

## 36. 开发结束后更新进度

### 提示词使用说明

一次有实质进展的开发会话结束时使用。进度必须以文件、Git、测试和验收证据为准。

自测结束须确认（v3.1.13）：临时桩已拆；产品口径修复已留下。不得把「测通了」当成「改过口径可以留着桩」。无拆桩/留下证据时，不得把自测写成已通过。

```text
本次开发工作结束。

请根据实际文档、Git、代码、测试和验收状态更新：

1. WORKLOG。
2. PROGRESS_REGISTER。
3. DOCUMENT_PROGRESS。
4. DEVELOPMENT_PROGRESS。
5. PROJECT_DASHBOARD。
6. BLOCKERS_AND_RISKS。

总结：

- 本次完成和未完成
- 修改文件
- Commit/PR
- 测试结果
- 对应需求、技术设计和 UI Contract
- 视觉验收结果
- 文档进度
- 开发执行进度
- 正式交付完成度
- 阻塞、风险和下一步准确操作
- 自测：临时桩是否已拆；产品口径修复是否留下；有无「为过验收改口径」痕迹

没有证据不得提高百分比。
无拆桩/留下证据时，不得把自测写成已通过。
若本次结束的是整个开发批次（准备标 Code Complete，或准备开始下一批），完成后继续第 36A 节，不得直接开下一批。
若已启用白话，第 36A 节之后再发第 36D 节，不要在本节写白话正文。
```

## 36A. 结束开发批次时收口 PRD

### 提示词使用说明

一个开发批次准备标 Code Complete / Closed，或准备开始下一批次前使用。
检查的是：本批实施中用户逐条确认并已落地的业务规则，是否已经写入当前 PRD。
一般性需求变更仍走第 40～42 节；从临时袋纳入走第 39C 节。本节是批次门禁，不替代 CR 流程。
没有此类改动时不要创建空 CR。
盘点只对照已入库的 PRD/DEC；未纳入的临时袋草稿不算已收入。
完成后若已启用白话（存在 `{{DOC_ROOT}}/progress/PLAIN_SYNC.yaml`），把本批触及的对照表行标 stale，再发第 36D 节。不要在本节写白话正文。尚未做第 36B 节则不要假装已同步。

自测临时桩的拆装不是 PRD 变更，不要列入本节。测出并留下的产品口径修复，仅当相对当前 PRD 出现新规则时才列入本节。

```text
当前阶段：BATCH_PRD_CLOSE

本次只处理刚结束或准备收口的批次：{{BATCH_ID}} {{BATCH_NAME}}

不要开始下一批次。
不要把本批实现细节写入 PRD。

请盘点本批「逐条改动」：

1. 本批用户已二次确认并已落地的业务规则、对象范围、状态、权限、计费或费率口径、验收口径。
2. 对照当前 Approved/Baselined PRD：哪些尚未收入。
3. 已写入 DEC 但未同步 PRD 的条目必须列入。
4. 排除纯实现：操作按钮、SQL 文件编号、接口字段名、不冻结的示例数据清单、临时代码路径。
5. 未纳入的临时袋草稿不算已收入 PRD。
6. 排除自测临时桩的拆装。测出并留下的产品口径修复，仅当相对当前 PRD 出现新规则时才列入。

若清单为空：
- 在 WORKLOG 写明「本批无待收口 PRD 变更」
- 更新看板下一步
- 不得创建空 CR

若清单非空：
- 起草一张 CR（可汇总本批已确认口径；每条高影响决策仍按第 2A 节逐项确认）
- 按第 40～42 节和第 2A 节完成
  CR_DECISION_REVIEW → CR_DECISION_CONFIRMATION → CR_DECISION_APPLY
- 收入 PRD：已 Approved/Baselined 则升补丁版本并保留旧基线；仍为 Draft 则同步当前稿并登记来源
- 更新 TRACE_MATRIX、OPEN_ITEMS、CHANGELOG 和进度看板

未完成盘点，或清单非空但 CR 尚未收入 PRD 前：
- 不得将下一批次升为 In Progress
- 不得声称本批业务口径已与 PRD 对齐

若存在 {{DOC_ROOT}}/progress/PLAIN_SYNC.yaml：
- 盘点完成后把本批触及的对照表行标为 stale
- 不要在本节写白话正文；同步发第 36D 节
```

## 36B. 白话文档第零批（只建骨架）

### 提示词使用说明

项目要对外白话说明、且还没有 `{{DOC_ROOT}}/progress/PLAIN_SYNC.yaml` 时使用。不是新的 G 阶段，不得改写 `PROJECT_STAGE`。不要塞进空项目初始化（G0 / 第 3 节）。

没有编写规格时停止：须先有 `{{DOC_ROOT}}/plain-spec/` 下的总体规划、地图编写说明、生成方案。本仓库这三份在 `docs/project/plain-spec/`。

本节只建目录、地图和空对照表，不写白话正文、不画架构图。没有本节完成的对照表，第 36C、36D 节都不要跑。

施工细则以 `{{DOC_ROOT}}/plain-spec/白话文档生成方案.md` 为准。先输出预检，等用户回复「按预检施工」再落盘。

```text
请完整加载并严格遵循：
@docs/governance/common_prd_v3.1/通用文档提示词v3.1.md
中的「主提示词」全文，以及
@docs/governance/common_prd_v3.1/通用文档提示词v3.1完整使用说明.md
中的第 2A 节、第 36B 节，以及
@docs/project/plain-spec/白话文档总体编写规划.md
@docs/project/plain-spec/plain-document文档地图编写说明.md
@docs/project/plain-spec/白话文档生成方案.md

这是白话文档第零批：只建目录和对照表，不写正文、不画图。

文档根目录：{{DOC_ROOT，默认 docs/project/}}
落地根目录：{{DOC_ROOT}}/plain-document/
对照表：{{DOC_ROOT}}/progress/PLAIN_SYNC.yaml
缺口文件：{{DOC_ROOT}}/progress/PLAIN_SYNC_GAPS.md

1. 按总体规划创建 {{DOC_ROOT}}/plain-document/ 目录树。
2. 按地图编写说明写入 doc-map.md，状态全为计划中。
3. 创建 PLAIN_SYNC.yaml：每一行对应总树一个文件，状态 planned。白话路径必须与总树一致。
4. 创建 PLAIN_SYNC_GAPS.md 空表。
5. 先预检列出将创建的路径，等我回复「按预检施工」再落盘。
6. 禁止写任何白话正文、禁止画架构图、禁止一次生成整套说明。
功能描述按第 2A 节人话 6 问。知情决策只在本窗口正文提问。
```

## 36C. 白话文档初次创建

### 提示词使用说明

对照表已存在，目标文件状态为 `planned`（或目录尚无正文）时使用。已是 `synced`/`stale` 的文件走第 36D 节。

必须写明本次文件清单。禁止一次生成整套，禁止写「第一批全部」或「全部 71 份」。清单从 `{{DOC_ROOT}}/plain-spec/白话文档生成方案.md` 批次表或总体规划复制具体路径。没有第 36B 节对照表时改走第 36B 节。

先输出预检，等用户回复「按预检施工」再落盘。施工细则以生成方案为准，并加载本批用到的分册编写方案。

本次清单若含架构图：流程图和泳道每个框必须是完整业务话，写清谁、对象/订单变成什么、钱或权限落到哪、本期没有的路标「无此路径」；关键判断用菱形，关键异常用分支。禁止「下单 / 支付 / 完成」这类空标题框。时序每条消息用人话，不写接口路径、字段名、类名。静态图框和箭头必须有人话含义，不要求写成订单步骤长句。图密度不够视为未完成，不得标 `synced`。

```text
请完整加载并严格遵循：
@docs/governance/common_prd_v3.1/通用文档提示词v3.1.md
中的「主提示词」全文，以及
@docs/governance/common_prd_v3.1/通用文档提示词v3.1完整使用说明.md
中的第 2A 节、第 36C 节，以及
@docs/project/plain-spec/白话文档总体编写规划.md
@docs/project/plain-spec/白话文档生成方案.md
（再 @ docs/project/plain-spec/ 下本批用到的分册：总览 / 功能需求 / 分端 / 技术设计 / 架构图 / 地图编写说明）

模式：白话初次创建。
本次清单：（从生成方案批次表或总体规划复制具体路径，不要写「第一批全部」）

规则从当前产品需求（v{{版本}}）及相关模块摘。
现码以当前分支 {{branch}} @ {{commit}} 的菜单脚本和现用页为准。
对照表：{{DOC_ROOT}}/progress/PLAIN_SYNC.yaml

先预检：将写文件（口令范围 ∩ planned）、将读真源/现码、冲突预备记缺口、明确不写什么。
等我回复「按预检施工」再生成。
正文零编号。待配置写未定。现码与口径不一致只记 {{DOC_ROOT}}/progress/PLAIN_SYNC_GAPS.md。
禁止超出口令清单。禁止把白话升格成 PRD。禁止反写正式文档。
架构图密度：流程图和泳道每个框必须是完整业务话（谁、变成什么、钱或权限落到哪、无此路径）；禁止空标题框。时序用人话、不写接口参数。静态图框和箭头必须有人话含义。密度不够不得标 synced。
写完更新对照表为 synced，填 prd_version 与 git_commit，更新 doc-map 状态列。
功能描述按第 2A 节人话 6 问。知情决策只在本窗口正文提问，一组最多 5 题。
```

## 36D. 白话文档差异同步

### 提示词使用说明

开发批次已完成第 36A 节、且对照表行上已有 `prd_version` 与 `git_commit` 时使用。只刷 `stale` 篇。

没有基线的行禁止当差异刷，改第 36C 节只补缺篇。对照表不存在时改第 36B 节。

禁止整夹重生、禁止用代码发明规则、禁止把白话升格成 PRD。口径变了才改功能需求白话；只是菜单/页面变了只改分端和图。

先输出预检，等用户回复「按预检施工」再落盘。施工细则以 `{{DOC_ROOT}}/plain-spec/白话文档生成方案.md` 为准。

刷架构图时同样遵守密度：流程图和泳道每个框必须是完整业务话；禁止空标题框。时序用人话、不写接口参数。静态图框和箭头必须有人话含义。密度不够不得标 `synced`。

```text
请完整加载并严格遵循：
@docs/governance/common_prd_v3.1/通用文档提示词v3.1.md
中的「主提示词」全文，以及
@docs/governance/common_prd_v3.1/通用文档提示词v3.1完整使用说明.md
中的第 2A 节、第 36A 节、第 36D 节，以及
@docs/project/plain-spec/白话文档总体编写规划.md
@docs/project/plain-spec/白话文档生成方案.md
（再 @ docs/project/plain-spec/ 下本批用到的分册编写方案）

模式：白话差异同步。
对照表：{{DOC_ROOT}}/progress/PLAIN_SYNC.yaml
基线：各 stale 行上的 prd_version 与 git_commit；没有基线的行禁止当差异刷，改第 36C 节。
本次要比的范围：{{相对基线变更的 prd/baseline/changes 与现码路径，或 git diff 说明}}

只刷命中的 stale 文件。先预检再等「按预检施工」。
口径变了才改功能需求白话；只是页面变了只改分端/图。
禁止用代码发明规则。冲突写入 {{DOC_ROOT}}/progress/PLAIN_SYNC_GAPS.md。
架构图密度：流程图和泳道每个框必须是完整业务话；禁止空标题框。时序用人话、不写接口参数。密度不够不得标 synced。
完成后 synced，更新版本与 commit，更新 doc-map。
功能描述按第 2A 节人话 6 问。知情决策只在本窗口正文提问，一组最多 5 题。
```

# 第七部分：人员交接

## 37. 开发者离开前交接

### 提示词使用说明

开发者离职、换人、长期暂停或跨团队转交时使用。交接文档不能代替 Git 和测试证据。

```text
开发者 {{FROM_DEVELOPER}} 即将把项目工作交给 {{TO_DEVELOPER}}。

当前批次：{{BATCH_ID}}

请检查：

1. Git 分支、Commit、PR 和工作区。
2. 未提交和未推送内容。
3. 当前批次和真实进度。
4. PRD、技术设计、API 和数据。
5. EFFECTIVE_UI_CONTRACT 和视觉资产（如启用）。
6. 代码和数据库迁移。
7. 测试和视觉验收结果。
8. 配置和 Feature Flag。
9. TODO、临时代码、已知问题、阻塞和风险。
10. 是否存在 {{DOC_ROOT}}/changes_temp（被 gitignore，Git 干净也可能有未入库决策）；有则写入交接，必要时把袋子拷走。
11. 若已启用白话：{{DOC_ROOT}}/progress/PLAIN_SYNC.yaml 是否有 stale、缺口文件是否有未闭环项。

生成 HANDOVER-XXX，并更新：

- HANDOVER_REGISTER
- DEVELOPMENT_PROGRESS
- PROJECT_DASHBOARD
- BLOCKERS_AND_RISKS

不得写入真实密码、Token、私钥和生产凭证。
```

## 38. 新开发者接管

### 提示词使用说明

接收人准备继续开发前使用。先验证项目和文档是否一致，不立即修改代码。

```text
开发者 {{TO_DEVELOPER}} 开始接管项目。

先不要继续开发。

请依次核对：

1. PROJECT_DASHBOARD。
2. 最新 HANDOVER。
3. 当前开发批次。
4. PRD、技术设计、API 和数据。
5. EFFECTIVE_UI_CONTRACT 和视觉资产（如启用）。
6. Git 分支、Commit、PR、未提交和未推送修改。
7. 本地启动。
8. 数据迁移。
9. 自动测试和视觉测试。
10. 已知问题和下一步。
11. 是否存在未纳入的临时变更袋 {{DOC_ROOT}}/changes_temp。
12. 若已启用白话：对照表 {{DOC_ROOT}}/progress/PLAIN_SYNC.yaml 与缺口文件。

输出：

- 交接是否完整
- 文档与代码是否一致
- 项目是否可启动
- 测试是否通过
- UI 资产是否齐全
- 当前真实进度
- 临时变更袋是否存在、是否已纳入
- 阻塞和风险
- 建议继续的具体任务

确认接管前不要修改业务代码。
```

# 第八部分：需求和设计变更

## 39. 资料投放期间补充资料

### 提示词使用说明

项目仍处于 INTAKE，用户继续增加需求、设计图或技术参考时使用。

```text
这是第 {{BATCH_NO}} 批补充资料。

资料路径：{{PATH}}
资料类型：{{TYPE}}
适用范围：{{SCOPE}}

请继续登记、摘要和识别冲突。
更新资料处理进度和项目看板。
当前仍为 INTAKE。
不生成最终 PRD、技术设计或正式 UI_SPEC。
```

## 39A. 初始化变更稿模式

### 提示词使用说明

文档架子已经建好、准备启用临时变更袋时使用。每个文档根目录铺一次即可。只写说明书、变更夹 README 入口和 gitignore，不写任何需求草稿。

空目录还没有文档架子时，先发第 3 节做普通初始化，不要用本节代替 G0。
不要把本节塞进 G0 必建清单。

```text
初始化变更稿模式。

请完整加载并严格遵循：
@docs/governance/common_prd_v3.1/通用文档提示词v3.1.md
中的「主提示词」全文，以及
@docs/governance/common_prd_v3.1/通用文档提示词v3.1完整使用说明.md
中的第 2A 节和第 39A 节。

文档根目录：{{DOC_ROOT，默认 docs/project/}}
临时袋目录：{{DOC_ROOT}}/changes_temp/（不提交 Git）

只做铺路，不写任何需求草稿：

1. 若还没有文档架子（无 {{DOC_ROOT}}/changes/），先告诉我去做第 3 节普通初始化，不要在这里编目录。
2. 写出 {{DOC_ROOT}}/changes/变更稿模式.md（通用写法，不写本项目业务规则）。
3. 变更夹 README 写明：正式变更是 CR-xxx.md；要用临时袋先看《变更稿模式》，或先发本节。
4. 忽略临时袋、不要提交。仓库已有 .gitignore 则加入 {{DOC_ROOT}}/changes_temp/；没有则创建并写入该行。
5. 不要创建临时袋内的 INDEX、BAG 或条目。
6. 不要修改已入库的 DEC/PRD/正式 CR 正文，不要修改业务代码。
7. 不要改写 PROJECT_STAGE。

说明书或 ignore 已存在且内容符合本节：汇报已铺过，不要重复生成一套。
做完汇报铺了或跳过了哪些文件。
```

## 39B. 进入变更稿模式

### 提示词使用说明

本会话只梳理需求、不改已入库正式文档、不改业务代码时使用。适用于：正式需求文档已经存在，且要和同事错开改这些文件。

还在投资料、还在写第一版需求时，应提醒直接写正式文档；用户坚持再用再进入。
说明书尚未铺过时，先执行第 39A 节，再进入本节。
每个项目第 39A 节只需一次；本节可多次进入（新窗口续写袋子）。

```text
进入变更稿模式。

请完整加载并严格遵循：
@docs/governance/common_prd_v3.1/通用文档提示词v3.1.md
中的「主提示词」全文，以及
@docs/governance/common_prd_v3.1/通用文档提示词v3.1完整使用说明.md
中的第 2A 节、第 39A～39C 节，以及
{{DOC_ROOT}}/changes/变更稿模式.md。

文档根目录：{{DOC_ROOT，默认 docs/project/}}
临时袋：{{DOC_ROOT}}/changes_temp/（不提交 Git）

若说明书或 gitignore 还没有，先按第 39A 节补上，再进入。
若尚无正式需求/决策文档可对照，先提醒：现在更适合写正式文档；我坚持后再进入。

本会话只允许：
- 知情决策仍按第 2A 节在本窗口正文提问（一组最多 5 个 ID）
- 功能描述按第 2A 节人话 6 问
- 已确认/未确认条目只写入临时袋（草稿编号 DRAFT-*，禁止预占正式 CR/DEC 号）
- 把用户指定的中转总结拆成袋内条目（拆条，不是整篇粘贴）
- 纯实现（按钮、SQL 编号、接口字段名）标成不迁入 PRD

本会话禁止：
- 修改已入库的 DEC/PRD/看板/正式 CR
- 修改业务代码、增量 SQL
- 把未确认条目写成已确认
- 发送第 35 节开工口令
- 改写 PROJECT_STAGE（袋内 BAG.md 记「变更稿开着」即可）

先建袋（若尚无）：INDEX.md、BAG.md，并汇报袋状态。然后等我下一条。
```

## 39C. 变更稿纳入正式文档

### 提示词使用说明

临时袋要释放到正式文档时使用。先 pull 同事代码、工作区干净。纳入不是第四条变更管道：按当前 PRD 状态走第 40、41 或 42 节。

未二次确认的条目不得写入正式文档。先出对照表，等用户确认后再 APPLY。
全部迁完且用户回复「纳入完成」后，才删除临时袋内文件（gitignore 规则保留）。

```text
变更稿纳入正式文档。

请只读：
- {{DOC_ROOT}}/changes_temp/INDEX.md 与全部 DRAFT 条目、锚点补丁
- 当前正式 DEC/PRD/CR 最大编号（git pull 之后的工作区）
- 第 2A 节、第 36A 节、第 40～42 节、{{DOC_ROOT}}/changes/变更稿模式.md

按当前 PRD 状态选择写入路径：
- Draft / In Review → 第 40 节
- Approved → 第 41 节
- Baselined → 第 42 节

规则：
1. 未二次确认的条目不得写入正式文档，保持未确认或当场补第 2A 节。
2. 已确认条目：按迁入头的目标路径和标题对照现稿；能合并则合并；冲突不能自决则知情决策，禁止默选。
3. 正式编号：看 changes/ 与 baseline 当前最大号往后编，禁止沿用会话里猜的下一号。空项目从 CR-001 / 该库决策号规则的第一号起。已占用号禁止改挂另一主题；发现整篇换题时按主提示词「编号占用规则」分类改引用，禁止全局替换。仍被引用的规则正文按「在引用规则保护」禁止删除、清空或整篇换题。
4. 纯实现条目不写入 PRD（第 36A 节），可进工作日志或开发口令清单。
5. 写入走对应节的 CR_DECISION_APPLY；已 Approved/Baselined 则保留旧基线只读，新版本从 Draft 起。
6. 未全部迁完、或仍有冲突未确认：不得删除 changes_temp。
7. 全部迁完并经我回复「纳入完成」后，再删除 changes_temp 内文件（gitignore 规则保留）。

先出对照表（草稿条 → 目标正式文件 → 冲突/可迁/不入库），停止。等我确认后再 APPLY。
```

## 40. PRD Draft/In Review 时新增需求

### 提示词使用说明

PRD 尚未 Approved 时新增或修改需求使用。需要创建 CR 并重新审计受影响模块。
开发批次结束时的逐条改动收口先走第 36A 节，再按本节或第 41、42 节依 PRD 状态写入。
从临时袋纳入的变更先走第 39C 节对照表，再按 PRD 状态进入本节或第 41、42 节。

```text
当前 PRD 处于 {{Draft/In Review}}。

新增或修改需求：
{{CONTENT}}

来源：
{{SOURCE}}

请：

1. 登记 SRC-CHANGE。
2. 创建 CR。
3. 做业务、技术、UI 和测试影响分析。
4. 对存在选择的内容按第 2A 节执行
   `CR_DECISION_REVIEW → CR_DECISION_CONFIRMATION → CR_DECISION_APPLY`。
5. 初次选择不形成 DEC，不修改 PRD。
6. 二次确认和受控应用完成后，更新 PRD、追踪矩阵和开放项。
7. 同步或预留 UI 目录变化。
8. 重新审计受影响模块。
9. 更新文档进度和项目看板。
10. 保持当前评审状态，不自动批准。
11. 新 DEC / ADR / CR / REQ 按当时最大号 +1；禁止改挂已占用号。若 pull 后发现已批准文件被整篇换题，先出编号占用对照表，按主提示词「编号占用规则」处理后再写入本 CR。改已有规则文件前先检索引用；仍按原主题被引用则不得整篇换题。
```

第 40～47 节中凡出现业务、版本、范围、兼容、迁移、数据保留、
灰度或回滚选择，统一复用：

```text
进入 CR_DECISION_REVIEW。
本次只读。按第 2A 节解释当前 CR 决策及完整影响；
初次选择后停止，不修改 PRD、技术文档、UI_SPEC、批次或状态。
```

```text
进入 CR_DECISION_CONFIRMATION。
本次只读。复述 CR ID、决策 ID、选择、限制条件、被拒方案、
业务和技术影响、迁移测试发布回滚影响及剩余 TBD。
等待我明确回复“确认该决策”。
```

```text
进入 CR_DECISION_APPLY。
本次只应用：{{CR ID、决策 ID、选择、限制条件}}。
按类型形成 DEC/ADR/UI-DEC，并只同步已确认范围内的文档和状态。
```

## 41. PRD Approved 后新增需求

### 提示词使用说明

PRD 已 Approved 但尚未或已经进入后续设计时使用。旧 Approved 版本必须保留。编号占用、整篇换题与在引用保护按主提示词「编号占用规则」「在引用规则保护」处理。

```text
PRD {{CURRENT_VERSION}} 已 Approved。

新增或修改需求：
{{CONTENT}}

请：

1. 保留当前 Approved 版本。
2. 登记 SRC-CHANGE。
3. 创建 CR。
4. 按第 2A 节解释补丁、次版本和主版本的差异、风险及迁移影响，
   完成二次确认后再确定版本级别。
5. 完成受控应用后创建新版本 Draft。
6. 分析 PRD、架构、技术设计、API、数据、UI 目录、
   UI_SPEC、开发批次、测试、发布和回滚影响。
7. 更新进度分母并记录范围变化。
8. 不静默覆盖旧版本。
```

## 42. Baselined 后变更

### 提示词使用说明

当前交付版本已经 Baselined 时使用。原基线只读，新版本重新流转。编号占用、整篇换题与在引用保护按主提示词「编号占用规则」「在引用规则保护」处理。

```text
当前版本 {{CURRENT_VERSION}} 已 Baselined。

变更内容：
{{CONTENT}}

请创建 CR，并评估：

1. PRD。
2. 总体架构。
3. 技术模块和详细设计。
4. API 和数据。
5. 迁移和兼容。
6. UI_SCOPE_MAP 和 UI_APPLICATION_MAPPING。
7. UI_SPEC 和图标资产。
8. 有效 UI Contract。
9. 开发批次和测试。
10. 发布和回滚。
11. 进度分母变化。

范围、兼容、迁移、数据保留和回滚存在选择时，按第 2A 节逐项处理；
初次选择不得修改原基线或新版本状态。

保留当前基线只读。
新版本从 Draft 开始。
```

## 43. 开发中发现问题

### 提示词使用说明

开发过程中发现实现、PRD、技术设计或 UI 规范不一致时使用。先分类，不能由开发人员直接修改业务规则。

```text
批次 {{BATCH_ID}} 发现问题：

{{DESCRIPTION}}

请判断：

1. 实现偏离 Approved PRD：缺陷。
2. 目标业务行为改变：CR。
3. PRD 有歧义：澄清型 CR。
4. 技术方案变化但业务不变：ADR 或技术设计变更。
5. 纯视觉规范变化：UI-DEC 和 UI_SPEC 新版本。
6. 设计图与 PRD 冲突：UI-CONFLICT，必要时升级 CR。
7. 原始图标缺失：资产阻塞或批准替代方案。

分类存在争议或需要用户选择处理路径时，按第 2A 节先解释各分类的
后果并完成二次确认；不得由开发人员或 AI 代选。

不要自行修改已批准业务规则。
争议未确认时，只登记“待分类”阻塞和风险，不提高进度或写入最终分类；
完成 `CR_DECISION_APPLY` 后，再更新最终分类、进度和项目看板。
```

## 44. UI 素材变更但业务不变

### 提示词使用说明

只替换设计图、图标、品牌色或组件视觉规范，且不改变业务字段、流程和权限时使用。

```text
本次是纯 UI 素材变更，业务目标和行为不变。

变更节点：{{UI_NODE_PATH}}
新素材路径：{{PATH}}
变更原因：{{REASON}}

请：

1. 登记新 SRC-UI。
2. 更新 MATERIALS_MANIFEST 或 ICON_MANIFEST。
3. 保留旧素材并标记 Superseded。
4. 执行文件哈希和版本比较。
5. 若存在替代方式、继承或验收取舍，先复用第 24 节 UI 决策三步骤。
6. 无决策分歧，或 `UI_SPEC_DECISION_APPLY` 已完成后，
   才生成受影响 UI_SPEC 的新 Draft。
7. 审计继承节点。
8. 更新受影响的 UI Contract 和视觉测试。
9. 不修改 PRD。

如果实际影响业务字段、流程、权限或状态，停止并升级为 CR。
对“纯 UI”与“业务变化”的边界存在争议时，按第 2A 节确认后再继续。
```

## 45. 只修改运营配置

### 提示词使用说明

调整已经批准模型内的配置值时使用。超出 PRD 上下限或改变权限模型时仍需 CR。

```text
配置变更：

配置项：{{KEY}}
原值：{{OLD_VALUE}}
新值：{{NEW_VALUE}}
生效时间：{{EFFECTIVE_AT}}

请检查是否仍在 Approved PRD 的配置范围内。

在范围内：
- 更新配置版本和审计
- 更新工作日志和项目看板
- 不创建业务 CR

突破金额、状态、权限、对象范围或上下限：
- 创建 CR
- 重新评审

是否超出 Approved 范围存在歧义时，按第 2A 节作为高影响决策处理；
确认前不应用配置变更。
```

## 46. 线上紧急变更

### 提示词使用说明

线上严重故障必须立即修复时使用。先执行最小修复，之后补齐完整文档和追踪。

```text
这是线上紧急变更。

问题：{{PROBLEM}}
影响：{{IMPACT}}
临时措施：{{MITIGATION}}

执行止损前必须存在用户或预先批准的事件响应机制给出的明确授权，
且授权至少包含：

- 故障对象
- 最大修改范围
- 回滚点

沉默、模糊表达或仅报告故障不构成执行授权。缺少上述授权时，
只允许诊断、评估和提出最小止损方案，不得修改生产或正式状态。

请创建 HOTFIX-CR，并给出：

1. 最小修复范围。
2. 风险。
3. 测试。
4. 发布。
5. 回滚。
6. 数据影响。
7. UI 和视觉回归影响。

完成后补齐：

- 来源
- DEC/ADR/UI-DEC
- PRD 或技术设计
- API/数据
- UI_SPEC 或 UI Contract
- 测试和视觉测试结果
- 追踪矩阵
- 工作日志
- 项目看板

紧急情况下可先执行已授权的最小止损，但事后补齐 DEC、ADR、UI-DEC
和长期方案时仍须按第 2A 节完成影响复述和二次确认。
```

## 47. 废弃已有功能

### 提示词使用说明

准备下线已经存在的业务功能、页面、接口或 UI 模块时使用。不得直接删除历史需求和设计目录。

```text
计划废弃：

需求编号：{{REQ_ID}}
功能：{{FEATURE}}
原因：{{REASON}}

请创建 CR，并分析：

1. 用户影响。
2. 数据保留。
3. API 兼容。
4. 页面入口和路由。
5. 存量业务对象。
6. 权限和通知。
7. 数据迁移。
8. UI 目录、UI_SPEC、图标和视觉基线。
9. 灰度和回滚。
10. 开发批次、测试和进度影响。

废弃范围、数据保留、兼容窗口、灰度和回滚方案按第 2A 节作为
高影响决策逐项确认；初次选择不得直接删除或下线。

不得删除历史需求、设计规范和素材登记。
旧内容标记 Deprecated 或 Superseded，并保留替代关系。
```

# 第九部分：推荐检查清单

## 48. 生成开发批次前

- PRD 是否 Approved/Baselined。
- 技术架构和模块设计是否完成。
- API、数据和编码规范是否有唯一真源。
- UI_SCOPE_MAP 是否与 PRD 同步。
- products/ 是否来自已确认的应用架构和 PROJECT_STRUCTURE。
- application_slug 是否映射真实 repository_path。
- 是否明确启用或跳过 UI 规范。
- 启用 UI 规范时，所需 UI_SPEC 是否 Approved/Baselined。
- 高保真页面的字体、图标、Logo 和设计版本是否齐全。
- 是否存在未解决的 UI-CONFLICT。
- 是否生成 EFFECTIVE_UI_CONTRACT。
- 所有高影响问答是否有第 2A 节影响复述、二次确认和受控应用证据。
- 是否误把取消问卷、未理解或初次选择记录为 DEC/ADR/CR/UI-DEC。
- 上一批次是否已完成第 36A 节：有待收口口径则 CR 已收入 PRD，否则已记录无待收口变更。
- 上一批次若已启用白话：第 36A 节完成后是否已按第 36D 节处理 stale（或明确本批未触及白话）。
- 若存在未纳入的临时变更袋，是否误把袋内口径当成已基线 PRD。
- 批次测试计划是否写明：不得为过验收改口径；临时桩测完拆掉；产品口径修复留下。灌库不算改口径。
- P0/P1 是否为 0。

## 49. UI 素材检查

- 每个素材是否有来源 ID。
- 是否登记版本、哈希、范围和状态。
- 原始素材是否与生成规范分开。
- SVG、PNG、WebP 等资产是否保留原文件。
- 标准组件库图标是否登记包名、版本和组件名。
- AI 重建图标是否放入 `reconstructed/`。
- AI 重建图标是否仍为 `Needs Review`。
- 被正式引用的 AI 重建或替代图标是否有知情决策二次确认证据。
- 小程序目标格式是否经过兼容验证。
- 版权或许可是否明确。
- 旧素材是否保留 Superseded 关系。

## 50. 开发完成前

- 代码是否映射到批次、需求和技术设计。
- 页面是否遵循 EFFECTIVE_UI_CONTRACT。
- 是否覆盖 Loading、Empty、Error、Disabled 和 No Permission。
- F3/F4 页面是否执行视觉验收。
- 是否误改非目标区域。
- API、数据、权限、状态和异常是否一致。
- 实施中新增的决策是否完成第 2A 节复述、二次确认和受控应用。
- 是否误把取消、不理解或初次选择作为实现依据。
- 本批逐条已确认业务改动是否已按第 36A 节盘点；有则 CR 已收入 PRD，无则已记录无待收口变更。
- 本批若已启用白话，对照表相关行是否已标 stale 并按第 36D 节同步（或记录本批未触及）。
- 测试、视觉测试、回滚和人工验证是否完成。
- 自测临时桩是否已拆；产品口径修复是否留下（未改规则则不进第 36A 节）。
- 是否为了过验收改了口径或把演示改成新规则。
- 进度是否有 Git、测试和验收证据。
- WORKLOG 和项目看板是否更新。

## 51. v3.1 最小使用路径

v3.1.15 不改变阶段编号。G9.5 禁止跳步：令牌确认后下一刀是第 28A 节，不是换皮；每步收工报当前节、下一节口令、禁止说成什么。
v3.1.14 不改变阶段编号。新增第 23A、25A、28A、32A 节：按应用分闸、anydesign 素材、Draft UI_SPEC、独立视觉修复循环。循环与 G9.5 均可单独使用。
v3.1.13 不改变阶段编号。自测不得为过验收改口径；临时桩测完拆掉；产品口径修复留下。细则见第 35、36 节，不塞进第 36A 节。
v3.1.12 不改变阶段编号。仍被引用的规则正文禁止删除、清空或整篇换题；新主题必须新号新文件。
v3.1.11 不改变阶段编号。已占用决策号禁止改挂；发现整篇换题时恢复旧主题到最大号 +1，按语义改引用，禁止全局替换。
v3.1.10 不改变阶段编号。对外白话架构图（流程图、泳道）必须按完整业务话密度画，禁止空标题框。
v3.1.9 不改变阶段编号。对外白话（第 36B～36D 节）是可选能力：三份口令（骨架、初次创建、差异同步），施工细则以项目 `plain-spec` 生成方案为准。空项目初始化不生成白话正文。没有对照表不得写正文；没有基线不得走差异。
v3.1.7 不改变阶段编号。变更稿（第 39A～39C 节）是可选分叉：双人同分支、要错开改正式文档时再用。不是每个新项目必做。空项目初始化只在变更夹 README 留入口，不生成说明书全文。
v3.1.5 不改变阶段编号。每个开发批次结束时先发第 36A 节：有逐条业务改动则起草 CR 并收入 PRD，无则记录后进入下一批。
v3.1.4 不改变阶段编号。新开对话窗口续做时，先发第 1A 节提示词，以项目文档进度为准。
v3.1.3 不改变阶段编号；在保留 G6/G7 强制参与门禁的基础上，
把第 2A 节知情决策三阶段应用到全部问答模式。默认 `MANDATORY` 模式下，
先解释、再初选、复述影响、二次确认和受控应用是最小路径的一部分；
只有完成知情决策确认的显式 `DIRECT_DRAFT` 才能带 TBD 跳过对应评审。

如果项目不需要 UI 约束，最小路径是：

```text
主提示词
→ 初始化
→ 资料投放
→ 基线
→ PRD
→ UI 逻辑范围同步
→ 总体架构参与模式确认
→ IDP 知情决策：解释、初选、影响复述、二次确认和应用（MANDATORY 默认）
→ 架构和技术设计
→ 生成正式 UI 应用目录
→ 跳过 UI 规范
→ 开发批次
→ 开发和验收
→ （可选，随时可插）第 32A 节点名视觉修复循环
→ 批次结束：有逐条业务改动则起草 CR 并收入 PRD
→ （可选）第 36B 节骨架 → 第 36C 节按清单写正文；之后批次第 36A 节后再发第 36D 节
```

如果项目需要高保真 UI，完整路径是：

```text
主提示词
→ 初始化
→ 资料投放
→ 基线
→ PRD
→ UI 逻辑范围同步
→ 总体架构参与模式确认
→ IDP 知情决策：解释、初选、影响复述、二次确认和应用（MANDATORY 默认）
→ 架构和技术设计
→ 生成正式 UI 应用目录
→ 第 23A 节按应用启用分闸
→ 第 25A 节 anydesign 解析设计图
→ 令牌确认（若有 Open Questions）
→ 第 28A 节生成 Draft UI_SPEC（用户预定页只记 Partial，不是换皮）
→ 第 29 节批准 UI_SPEC
→ 点名页换皮（须另开口令；禁止业务路由 /v2）
→ 生成 EFFECTIVE_UI_CONTRACT
→ 开发批次
→ （随时可点名）第 32A 节视觉修复循环
→ 视觉回归和业务验收
→ 批次结束：有逐条业务改动则起草 CR 并收入 PRD
→ （可选）第 36B 节骨架 → 第 36C 节按清单写正文；之后批次第 36A 节后再发第 36D 节
```

## 52. 最终原则

```text
PRD 决定业务做什么
技术设计决定系统怎样实现
UI_SPEC 决定分层视觉和交互规则
EFFECTIVE_UI_CONTRACT 决定当前批次实际遵守什么
原始素材提供证据，但不能绕过评审直接控制开发
知情决策协议决定用户如何在充分理解影响后作出并应用选择；功能描述按第 2A 节人话 6 问
需要错开改正式文档时，变更稿决定需求先放临时袋还是直接写正式变更
对外白话（如启用）只投影已确认规则和落地页面，不能成为 PRD；对照表在 progress，不进开会包
白话架构图（流程图、泳道）用完整业务话，禁止空标题框
自测不得为过验收改口径；临时桩测完拆掉；产品口径修复留下
UI 规范按应用分闸，未列入的应用不算正式 UI_SPEC
anydesign 只产素材，批准前不能约束开发
令牌确认后下一刀是第 28A 节；未批准不得把点名换皮说成下一步
做完 G9.5 每一步必须报：当前节 / 下一节口令 / 禁止说成什么
视觉修复循环可随时点名，不占 G 号，未点名不得改页面
```
